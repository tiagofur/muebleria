# #805 — G-ODD v1.1 state and catalog simplification

## Objective

Deliver one isolated factory/process pull request that reduces routine startup
context and duplicated state by removing the global `progress/current.md`, making
the issue-scoped ODD artifact the only durable execution record for substantial
ODD work, and replacing the implementation ledger in `feature_list.json` with a
small catalog of genuine high-level product capabilities.

## Problem

The current factory correctly treats GitHub, code, tests, and exact candidate
evidence as authoritative, but two legacy surfaces still impose avoidable cost:

- `progress/current.md` duplicates live issue/PR state and requires conflict-prone
  cleanup after work finishes.
- `feature_list.json` contains 215 implementation rows across 477,954 bytes and
  5,888 lines, including fixes, refactors, reviews, tests, slices, execution state,
  and historical evidence that belong in Git and GitHub.

Routine `factory_preflight.py` still parses the catalog on every start. Live
factory instructions also retain references to the removed global ledger, and
some factory tests pin issue-specific historical narrative instead of reusable
semantic contracts.

## Why

G-ODD v1.1 should preserve feature continuity without session continuity: one
issue maps to at most one durable execution artifact, while GitHub Issues/PRs own
queue, approval, and global state; code/tests own implemented truth; and Engram is
only an optional mirror. Removing redundant state reduces startup tokens,
conflicts, stale status, and administrative commits without weakening G-ODD v1.0
controls.

## Scope

- Delete `progress/current.md` and remove it from live factory/process
  instructions, scripts, tests, documentation, and startup behavior.
- Define `odd/tasks/<issue>-<slug>.md` as the sole durable execution artifact for
  substantial ODD work. Direct remains artifact-free; explicit SDD uses only its
  canonical artifact.
- Keep completed ODD issue artifacts in `odd/tasks/` as immutable history that is
  never scanned globally or loaded at startup.
- Simplify `feature_list.json` into a compact, stable catalog of genuine product
  capabilities with no queue, scheduler, ownership, reservation, priority, or
  execution state.
- Align factory entrypoints, role skills, verification guidance, CI validation,
  and semantic drift tests with the reduced state model.
- Report exact before/after bytes, lines, catalog entries, and routine startup
  read paths.

## Non-goals

- Product behavior, product source logic, migrations, generated contracts, or
  product tests.
- Rebuilding the factory, adding a scheduler/dispatcher/daemon, enabling
  receipt-driven review, or changing existing product verification requirements.
- Creating `odd/archive/`, moving artifacts after merge, or requiring a later
  cleanup commit.
- Adding `scripts/factory_status.py`. It cannot distinguish active work from
  retained historical artifacts offline without duplicating GitHub/ownership
  state or introducing stale status metadata.
- Adding front matter to ODD artifacts. The filename already identifies the issue
  and the directory already identifies the ODD lane.
- Merging the PR, closing #805 manually, force-pushing, bypassing gates, or
  granting protected labels without current maintainer authority.
- A full review or rewrite of the broader `docs/` tree beyond live contradictions
  directly caused by this process change.

## Constraints

- Approved issue: #805 (`status:approved`), open at authorization time.
- Branch: `refactor/805-g-odd-v1.1`.
- Exact base: `origin/main@e6506e2608f784a80172888b4f1d27f056fcd651`.
- One writer owns implementation; a different fresh reviewer inspects the exact
  HEAD/base after the candidate is frozen.
- Strict TDD is enabled by the session instruction. Observe RED before behavior
  changes, then GREEN, then REFACTOR; never invent evidence.
- Preserve one writer, fresh independent review, V0/V1/V2, CI as validation,
  truthful publication metadata, exact HEAD/base, human merge,
  ownership/reservation, the one-shot principle, and Gentle-present/
  Gentle-absent portability.
- Historical evidence is not bulk-rewritten merely to erase old terminology.
  Drift guards distinguish live factory authority from archives and immutable
  historical records.
- Technical artifacts remain concise English. Existing product-language
  conventions are unchanged.

## Authorized scope

Factory/process surfaces only:

- `progress/current.md`
- `feature_list.json`
- `odd/tasks/` conventions and this issue artifact
- `AGENTS.md`, `CHECKPOINTS.md`, `README.md`, `init.sh`
- `.agents/skills/leader/SKILL.md`
- `.agents/skills/implementer/SKILL.md`
- `.agents/skills/reviewer/SKILL.md`
- `docs/demo/software-factory-*`
- `docs/verification.md`, `docs/git-workflow.md`
- directly contradictory live process/catalog statements in current docs
- `.github/workflows/ci.yml`
- factory/catalog/preflight/CI-result scripts and their focused tests

Archives, historical progress evidence, frozen audit snapshots, and unrelated
product files remain outside scope unless the approved issue explicitly names a
purely documentary live reference that cannot otherwise be removed safely.

## Acceptance criteria

- `progress/current.md` no longer exists, and no live factory/process authority
  reads, writes, recommends, or depends on a global current ledger.
- Routine startup reads the approved issue, role/contract, ownership, and affected
  sources only; `factory_preflight.py` does not parse `feature_list.json` or any
  global progress ledger.
- Substantial ODD uses exactly one `<issue>-<slug>.md`; Direct creates none;
  explicit SDD creates no duplicate ODD artifact; no session/review-round files
  are introduced.
- Completion requires no post-merge cleanup: the final issue artifact stays in
  `odd/tasks/` as immutable, non-startup history.
- `feature_list.json` contains only consolidated high-level product capabilities
  and minimal stable catalog metadata. It contains no bugs, CSS corrections,
  reviews, fixes, tests, implementation slices, microchanges, queue/scheduler/
  ownership data, or execution state.
- A deterministic schema/validator rejects operational fields or categories and
  duplicate/invalid capability identifiers.
- Removed operational history remains recoverable from Git, Issues, and PRs; no
  critical product capability is lost during consolidation.
- No `factory_status.py`, `odd/archive/`, artifact front matter, product behavior,
  merge, or manual issue closure is introduced.
- G-ODD v1.0 controls remain explicit and protected by focused semantic tests.
- Final reporting records exact before/after catalog bytes, lines, and entries;
  confirms removal of the 7,226-byte/107-line `current.md`; and names every path
  removed from routine startup reads.
- Focused factory tests, syntax/schema checks, proportional planning, exact-head
  CI, and fresh independent review are reported honestly for the frozen candidate.

## Execution tasks

- [x] **GODD11-1 — Pin the reduced-state contract with failing semantic tests**
  - Route: delegated direct, one bounded writer.
  - Trigger evidence: mapping covered more than four files; test preparation is
    preparation for a multi-file write; strict TDD requires observed RED before
    implementation.
  - Outcome: reusable tests fail because the global ledger still exists, routine
    preflight still parses the catalog, artifact naming is not fully issue-scoped,
    and the catalog still accepts operational implementation metadata.
  - Evidence: the exact focused runner executed 37 tests and failed 12 against the
    baseline behavior. Failures cover the missing catalog-independent preflight,
    legacy catalog schema, live global-current file/references, incomplete retained
    artifact lifecycle, non-issue filename, and startup catalog parsing.
  - Commit: pending creation; the immutable SHA will be recorded in GODD11-2's
    artifact update because a commit cannot contain its own identity.
  - Rollback boundary: focused factory-test changes only.

- [ ] **GODD11-2 — Remove global progress state and finalize ODD lifecycle**
  - Route: delegated direct, same sole writer.
  - Trigger evidence: coordinated deletion and alignment across entrypoints, role
    skills, ODD conventions, verification docs, and startup logic touches multiple
    non-trivial process files.
  - Outcome: `progress/current.md` is deleted; live process references disappear;
    issue-scoped artifacts remain in place after completion without archive moves,
    cleanup commits, front matter, or global directory scans; focused tests reach
    GREEN and are refactored without weakening assertions.
  - Rollback boundary: global-progress deletion, ODD lifecycle wording, startup
    dependency removal, and their tests; no catalog consolidation.

- [ ] **GODD11-3 — Replace the implementation ledger with a capability catalog**
  - Route: delegated direct, same sole writer.
  - Trigger evidence: all 215 entries require one semantic audit and consolidation;
    the schema, validator, CI, setup, docs, and focused tests must change together.
  - Outcome: genuine product capabilities are preserved or consolidated, operational
    history is removed, the deterministic validator rejects prohibited metadata,
    and focused tests complete RED → GREEN → REFACTOR.
  - Rollback boundary: capability catalog, validator/CI/setup consumers, catalog
    documentation, and their tests; ODD lifecycle remains independently coherent.

- [ ] **GODD11-4 — Freeze and prove the single process candidate**
  - Route: delegated verification after the sole writer completes normalization.
  - Trigger evidence: test/build execution and fresh independent review are separate
    bounded actors; exact candidate evidence must not inflate the parent context.
  - Outcome: work-unit commits record focused results and rollback boundaries;
    final metrics, V0/V1/V2 applicability, exact HEAD/base, CI, publication metadata,
    and limitations are ready for fresh human/ChatGPT review.
  - Rollback boundary: evidence and publication preparation only; no merge.

## Verification plan

### Strict TDD runner

- `python3 -m unittest discover -s scripts -p 'test_factory_*.py' -v`
  - RED: new semantic guards fail against the pre-v1.1 factory.
  - GREEN: the same command passes after each bounded behavior change.
  - REFACTOR: rerun after deduplicating validator/test logic.

### V0 — Structural

- `git diff --check` — clean whitespace and patch structure.
- `bash -n init.sh` — setup script syntax.
- `python3 -m json.tool feature_list.json >/dev/null` — valid JSON.
- Deterministic catalog validator command selected by the implementation — schema,
  unique IDs, allowed keys, and prohibited operational metadata.
- Live-reference guard — deleted `progress/current.md` and no active process
  authority references; archives/history excluded explicitly.
- `python3 scripts/factory_preflight.py --require-clean` — read-only clean final
  preflight returning `PREFLIGHT_OK_NOT_VERIFIED` without reading global ledgers.

### V1 — Functional factory behavior

- `python3 -m unittest discover -s scripts -p 'test_factory_*.py' -v`
- `python3 -m unittest discover -s scripts -p 'test_ci_*.py' -v`
- `python3 scripts/verify_affected.py --base origin/main --plan`
- Exact before/after byte, line, entry, and startup-read measurements.

### V2 — Operational

- `N/A` for product runtime: this issue changes factory/process behavior only.
- GitHub issue/PR metadata, exact-head CI, ownership, independent review, and human
  merge remain real remote boundaries and must be read back rather than inferred.

## Delivery forecast

- Forecast: approximately 6,500–7,200 authored additions plus deletions, dominated
  by the honest removal/consolidation of the 5,888-line feature ledger and the
  107-line global current file; generated output excluded.
- Requested delivery strategy: `single-pr`, matching the explicitly requested one
  isolated G-ODD v1.1 factory/process PR.
- Review budget: exceeds the approximately 400 authored-line heuristic.
- Required decision: the maintainer explicitly authorized the single-PR
  `size:exception` for issue #805 on 2026-09-20. The implementation may use that
  delivery strategy, but the parent still owns any protected GitHub label mutation.
- Rationale: splitting the catalog deletion from its schema, consumers, docs, and
  drift guards would create intermediate contradictory truth models. The large diff
  is overwhelmingly deletion of redundant historical implementation detail, not a
  license to omit tests or compress documentation.
- Chain strategy: none while `single-pr` is requested; if the maintainer declines
  `size:exception`, return for an explicit delivery-strategy decision rather than
  choosing a chain silently.

## Progress and evidence

- 2026-09-20: read-only mapping completed against exact
  `origin/main@e6506e2608f784a80172888b4f1d27f056fcd651` using CodeGraph first and
  targeted repository reads afterward.
- 2026-09-20: measured `progress/current.md` at 7,226 bytes / 107 lines and
  `feature_list.json` at 477,954 bytes / 5,888 lines / 215 entries
  (209 done, 5 pending, 1 in progress).
- 2026-09-20: confirmed an existing 269,795-byte pre-reset current snapshot plus
  Git history, so another archive copy adds no traceability value.
- 2026-09-20: identified routine preflight parsing of `feature_list.json`, live
  process references to `progress/current.md`, operational catalog consumers, and
  issue-specific historical assertions in the factory contract tests.
- 2026-09-20: selected in-place immutable artifact retention, no
  `factory_status.py`, and no artifact front matter as the lowest-cost deterministic
  design.
- 2026-09-20: created this execution artifact before any other source/process edit.
- Engram recovery mirror: `odd/805-g-odd-v1-1/tasks` in project `muebleria`.
- Source/process implementation: not started.
- Work-unit commits: none.
- 2026-09-20: maintainer explicitly authorized the single-PR `size:exception`;
  no GitHub label was mutated by the writer.
- 2026-09-20: GODD11-1 RED observed with
  `python3 -m unittest discover -s scripts -p 'test_factory_*.py' -v`: 37 tests,
  12 failures, exit 1. Existing non-target factory tests remained green; failures
  were the new reduced-state contract guards.

## Next step

Create the GODD11-1 RED work-unit commit, record its immutable identity in the next
artifact update, then implement GODD11-2 until the focused runner is GREEN for the
global-progress lifecycle while catalog-specific RED remains for GODD11-3.
