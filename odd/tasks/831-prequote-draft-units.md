# #831 — Prepare draft units when Design starts, without Q1

## Objective and authority

Implement the approved child of #398 so the normal React Design action prepares canonical pending FurnitureInstances for existing draft project lines before Q1. Preserve commercial and Design state; prove the HTTP, PostgreSQL/RLS, and real Chromium boundaries. Parent #398 retains integrated SketchUp-to-Engineering evidence.

- Issue: [#831](https://github.com/tiagofur/muebleria/issues/831), OPEN and `status:approved` at the 2026-09-23 readback; no assignee or open PR. The parent explicitly authorized one writer and a PR for human review, not merge or autoapproval.
- Worktree/branch: `/Users/tiagofur/.codex/worktrees/issue-831-prequote/muebles`, `fix/831-prequote-units`; clean baseline `origin/main@1484d20a2912e9132932c74653c67ffb1ed5d78b`. Recheck ownership and remote state before product writes or publication.
- Isolation: #642 nullable commercial-summary correction is a separate candidate and is **not** on this base. Do not read/write the active #469 GLM worktree or its files. No SketchUp installation, Keychain/session manipulation, `showroon.skp`, PTX/CNC, or unrelated owner changes.

## Problem and bounded scope

The observed React-first draft contained one `MOD-GAB-01` and one `MOD-CAJ-01` line, quantity 1 each, then a Design created through React; its furniture matrix still showed zero active/pending units. Read-only app-role/RLS inspection found two `project_items`, zero FIs/links/Q revisions, and an empty manual WorkingCopy. The authenticated HTTP request/response has **not** yet been captured. Existing `CreateDesign` and handoff do not invoke #385/#386 materialization. Explicit Q1-first behavior has **not** failed because Q1 was never invoked.

The correction owns draft-to-Design preparation and, only if necessary, the same idempotent preparation before handing off an existing Design. Reuse the canonical FI/link/quantity operations. Keep line IDs, amounts, material/dimension/configuration, existing FI IDs, Design and WorkingCopy intact. Coordinate draft edits, retries, double clicks and lost responses without duplicate or partial materialization; fail safely on ownership, concurrency, retirement, and durable history.

Do **not** create/issue/send/accept Q1, change `Project.status`, synthesize totals or commercial fields, mutate Q history, add a materialization button, or make GET/list/history reads write. Do not implement #390 insertion or full SketchUp autosync/duplication. A later explicit canonical Q1 must reuse the same unit IDs.

## Route, TDD, and delivery

- ODD route: delegated direct, with this one recovery artifact. Mapping spans React, API, PostgreSQL/RLS, generated contract, and browser (4+ files); the writer owns the multi-file source change. ~400 authored additions plus deletions per task is **advisory**, never a reason to omit tests, code-golf, or split a coherent behavior artificially.
- Strict TDD: **enabled** by repository `AGENTS.md` (`gentle-ai:strict-tdd-mode`). For each implementation slice: write a focused failing regression, observe **RED** against a fresh disposable PostgreSQL environment, implement, observe **GREEN**, then refactor/check. Record actual commands, output, and candidate SHA; no invented result.
- Safe Go runner: create a **fresh** `postgres:16-alpine` container with a generated password, dynamic loopback port and throwaway `granete_test_831` database; mount `scripts/postgres-init-app-role.sh` read-only, verify runtime role `LOGIN/NOSUPERUSER/NOBYPASSRLS`, export `DATABASE_URL=postgres://postgres:<generated>@127.0.0.1:<dynamic>/granete_test_831?sslmode=disable` to the test process only, then `cd backend-go && go test ./internal/storage -run '^(TestPrequoteDesignPreparation|TestQuoteLineFurniture_|TestQuoteLineMaterializeHTTP_Postgres|TestQuoteLifecycle_CreateInitialRevision_)' -count=1 -v`. Add the planned `TestPrequoteDesignPreparation` regression before the first run. The existing storage fixtures create/drop their own secondary test DB **inside that same disposable container**; destroy the whole container at exit. Never point tests at `localhost:5445/muebles`, the live #398 host container, or any persistent database. A skipped DB test is not GREEN.

  ```bash
  set -euo pipefail
  set +x
  ROOT="$(git rev-parse --show-toplevel)"
  C="granete-831-go-$$-$(openssl rand -hex 4)"
  trap 'docker rm -f "$C" >/dev/null 2>&1 || true' EXIT
  PG_PASSWORD="$(openssl rand -hex 32)"
  APP_PASSWORD="$(openssl rand -hex 32)"
  docker run -d --rm --name "$C" -e POSTGRES_DB=granete_test_831 \
    -e POSTGRES_USER=postgres -e POSTGRES_PASSWORD="$PG_PASSWORD" \
    -e APP_DATABASE_PASSWORD="$APP_PASSWORD" \
    -v "$ROOT/scripts/postgres-init-app-role.sh:/docker-entrypoint-initdb.d/10-app-role.sh:ro" \
    -p 127.0.0.1::5432 postgres:16-alpine >/dev/null
  for i in $(seq 1 120); do
    if docker logs "$C" 2>&1 | grep -Fq 'PostgreSQL init process complete; ready for start up.' && \
       docker exec "$C" pg_isready -U postgres -d granete_test_831 >/dev/null 2>&1; then break; fi
    sleep 1
  done
  docker logs "$C" 2>&1 | grep -Fq 'PostgreSQL init process complete; ready for start up.'
  docker exec "$C" pg_isready -U postgres -d granete_test_831 >/dev/null
  test "$(docker exec "$C" psql -At -U postgres -d granete_test_831 \
    -c "SELECT rolcanlogin, rolsuper, rolbypassrls FROM pg_roles WHERE rolname='granete_app'")" = 't|f|f'
  PG_PORT="$(docker inspect -f '{{(index (index .NetworkSettings.Ports "5432/tcp") 0).HostPort}}' "$C")"
  export DATABASE_URL="postgres://postgres:$PG_PASSWORD@127.0.0.1:$PG_PORT/granete_test_831?sslmode=disable"
  (cd "$ROOT/backend-go" && go test ./internal/storage \
    -run '^(TestPrequoteDesignPreparation|TestQuoteLineFurniture_|TestQuoteLineMaterializeHTTP_Postgres|TestQuoteLifecycle_CreateInitialRevision_)' \
    -count=1 -v)
  ```
- Authenticated HTTP proof: extend the real-PostgreSQL HTTP storage suite or an equivalent API integration test to issue the authenticated Design preparation request with a real app actor and verify status/body, cross-tenant rejection, RLS-visible rows, rollback and retry. Run its focused `go test ./internal/storage -run '^TestPrequoteDesignPreparation' -count=1 -v` under the same fresh-container runner; record the actual endpoint once defined, without credentials in logs.
- Real Chromium runner: `scripts/organization-browser-gate.sh tests/organization/prequote-design.spec.ts` (planned focused spec). This script creates/destroys its own disposable PostgreSQL, real Go backend, web app and Chromium session. The spec must create customer/project/two lines through the normal UI, capture sanitized authenticated request/response, create/reopen Design, and assert two distinct pending FIs with zero modeled items/Q revisions; no direct API/SQL business seeding to mask the entry point. Existing API-seeded organization specs remain supporting regression checks, not substitutes for this path.
- Delivery strategy: **`single-pr`**, explicitly selected by the user on 2026-09-23; `size:exception` on #831/corresponding PR is authorized **if the existing gate needs it**, without altering that gate. One PR keeps the pre-Q1 server operation, generated API, normal React invocation, and browser proof reviewable as one coherent behavior; a backend-only PR would not satisfy the approved UI acceptance. The ~400-line heuristic does not justify shrinking tests or code. Final additions plus deletions: **246 product/contract, 785 test, 95 task-document, 1 generated**; **1,126 authored** with generated excluded. Existing PR metadata/CI gates do not inspect this heuristic or require a label, so no protected `size:exception` label was changed. Commit coherent tested work units with Conventional Commits, no AI attribution. No push/PR until the authorized publication step; no merge.
- Receipt-driven review: do not enable it on the user's behalf. Determine current effective mode before any candidate review. A different fresh reviewer evaluates exact HEAD/base after functional proof.

## Actionable tasks

- [x] **T1 — Reproduce and fix the authoritative preparation boundary.** Capture the missing authenticated Design HTTP transition on disposable PostgreSQL; add RED regression covering two draft lines, quantity >1, prior materialization, retry/concurrency, concurrent draft edit, rollback, owner/RLS denial, terminal/durable-history protection, unchanged commercial fields, zero Q, and preserved WorkingCopy. Implement the smallest transactional/idempotent server operation using #385/#386. Record RED/GREEN/refactor, exact runner, and a coherent work-unit commit.
- [x] **T2 — Connect normal React intent and browser proof.** Wire existing Create Design (and existing-Design handoff only if needed) to the authoritative operation without a technical button or implicit Q1. Add a real UI-created Chromium regression with sanitized HTTP response, reload/readback, exactly two pending units and unchanged commercial state; cover failed/retried response. Run targeted UI/contract checks and commit this coherent work unit.
- [ ] **T3 — Verify Q1 reuse and handoff candidate.** In a fresh disposable DB, prove later explicit canonical Q1 reuses FI IDs/quantities without rewriting commercial history, run affected design/quote tests and generated-contract drift checks, inspect `verify_affected.py --base origin/main --plan`, then run applicable frozen-candidate checks. Obtain independent exact-head review and current CI only after publication; report remaining #398 SketchUp/Q/R/Engineering scope as **NOT_RUN**, not implied PASS.

## Acceptance, current evidence, and next step

Acceptance is the exact #831 issue contract: two quantity-1 draft lines yield two distinct pending units and no modeled units/Q; quantity N and existing units converge without ID churn; retries and concurrent edits neither duplicate nor partially prepare; foreign actors cannot prepare/read; commercial fields, Design/WorkingCopy and accepted history remain intact; later explicit Q1 reuses those IDs. Browser and app-role/RLS proof are mandatory. SketchUp placement and later #398 stages are outside this issue and require separate integrated evidence.

At artifact creation, `python3 scripts/factory_preflight.py` returned `PREFLIGHT_OK_NOT_VERIFIED`, tests `NOT_RUN`, on clean base. CodeGraph was initialized for this worktree before source mapping.

### T1 implementation checkpoint — 2026-09-23, uncommitted

- **RED:** The focused two-line test on a fresh disposable `postgres:16-alpine` database failed with `0 active/0 pending` instead of `2/2`. After adding the existing-Design HTTP test, the same disposable runner failed `POST .../draft-units:prepare` with `404 page not found`. The first attempted route shape (`{designId}:prepare-draft-units`) caused Go ServeMux to reject the wildcard segment; the implemented route uses a separate static `draft-units:prepare` segment.
- **Implementation:** `CreateDesign` takes a project `FOR UPDATE` lock and, for a draft without a QuoteRevision/source-Q, prepares current lines through the existing `MaterializeQuoteLine` command in the same tenant transaction. An explicit existing-Design handoff command and idempotent authenticated POST use the same helper, never a GET side effect. The helper locks line rows `FOR SHARE` and preserves Project/items, Design WorkingCopy, commercial values, and Q history. The generated client exposes the endpoint for T2. No Q1/status change.
- **GREEN:** In a new throwaway PostgreSQL container, `go test ./internal/storage -run '^(TestPrequoteDesignPreparation_|TestDesigns_|TestQuoteLineFurniture_|TestQuoteLifecycle_CreateInitialRevision_|TestQuoteLineMaterializeHTTP_Postgres)' -count=1 -v` passed **36 tests**, including eight new prequote tests (two lines, quantity/prior units/concurrent Designs, signed HTTP/replay/RLS, explicit Q1 ID reuse, rollback on durable history, and existing-Design storage/HTTP handoff). `go test ./internal/api -run '^(TestHandleProjectDesigns_|TestRespondWithDesignError)' -count=1` passed. Each Go run used a fresh dynamic-loopback `postgres:16-alpine` container, verified app role `LOGIN/NOSUPERUSER/NOBYPASSRLS`, and stopped the container at exit; no persistent DB was used. `python3 scripts/check_openapi_drift.py` passed (including negative operation proofs); `git diff --check` passed. Final `factory_preflight.py` remains `PREFLIGHT_OK_NOT_VERIFIED`, as it does not run tests.
- **Still pending:** Normal signed-in Chromium request/response was not captured; test HTTP is a signed in-process server request, not UI proof. The concurrent Design-create test is not a separate concurrent draft-*edit* test. T1 is therefore **not checked off**. No real browser/SketchUp, exact-head review, CI, PR, or commit claimed. The user has since selected `single-pr` with a conditional `size:exception`; the ~400-line task heuristic remains advisory, not an omission mandate.

The checkpoint above is historical. Current evidence and remaining work follow; do not infer #398 host/Q/R/Engineering outcomes from this backend/browser slice.

### T1/T2 continuation — 2026-09-23, before candidate commit

- **Concurrent draft edit:** `TestPrequoteDesignPreparation_ConcurrentDraftEdit` holds a project update/quantity edit open, starts Design creation, observes that Design cannot cross the uncommitted edit, then commits and verifies exactly two distinct units for quantity 2. A subsequent quantity-3 edit plus explicit existing-Design handoff retains both prior IDs and creates one delta. This regression was added after the project-lock implementation, so it has **GREEN but no separately observed RED**; do not infer a RED result. Existing rollback and durable-history tests pass.
- **Real React-first Chromium:** `bash scripts/organization-browser-gate.sh tests/organization/prequote-design.spec.ts` passed **2 tests** against fresh disposable PostgreSQL, real Go/web and Chromium. The customer, project, two quantity-1 draft lines, Design and later explicit Q1 were created through signed-in UI, not API/SQL business seeding. A supported catalog API call supplies only test catalog prerequisites. Browser capture: `POST /api/projects/{id}/designs` **201**, `GET /api/projects/{id}/designs` **200**, selected-Design `POST /api/projects/{id}/furniture-workspace` **200** with `2 active / 2 pending / 0 placed`; the React matrix and reload show the same two IDs. Read-only app-role/RLS transactions confirm two project lines, two distinct FI/link IDs, zero QuoteRevisions and modeled WorkingCopy items, and byte-equal Project/line JSON before and after preparation. Explicit UI Q1 `POST /api/projects/{id}/quote-revisions` **201** reuses the same FI/link IDs without changing the Project/line JSON. No credential or response body was logged.
- **Frozen-byte browser rerun:** After the last React source normalization, the same gate again passed **2/2 Chromium tests in 22.0s** and printed `[organization-gate] PASS`; the UI request statuses and app-role assertions above remain true for the final source bytes.
- **Existing-Design handoff/retry:** A second signed-in UI case creates a Design while the draft is empty, then adds two lines. `Open in SketchUp` sends the explicit `POST .../draft-units:prepare`; a simulated lost browser response after a real server **200** leaves two durable units but no pairing sheet and shows a retryable error. Retrying receives **200**, opens the pairing sheet, and reload preserves the same IDs, zero Q and empty WorkingCopy. This proves the browser pairing *entry*, not the SketchUp host itself.
- **React TDD/checks:** Focused unit test observed **RED** because the preparation-error alert was absent and pairing opened too early; after wiring the generated API command, `pnpm --filter @granete/ui exec vitest run src/digitalThread/ProjectDesignsScreen.test.tsx --reporter=dot` passed **58/58**. `pnpm typecheck` passed all workspace packages; `pnpm test` passed, including apps/web **536/536**. `python3 scripts/check_openapi_drift.py`, `gofmt -l` and `git diff --check` passed. The React tests and typecheck were rerun after the final handler normalization.
- **Broad Go suite finding and fixture correction:** The first fresh-PG `go test -p 1 -timeout=30m ./internal/storage ./internal/api` passed API but failed three existing storage tests: furniture workspace expected 5 units and got 6; two workshop occurrence tests expected frozen/current equality but saw an extra current unit. Their shared RLS fixture carried an unrelated qty-1 draft line that #831 now correctly prepares at Design creation. The two exact-unit fixture setups now remove that unrelated line before Design creation; no production behavior was weakened. The three failures plus new #831 tests passed a focused rerun on a **new** disposable PG (**12.47s**). A second **fresh** disposable PG ran the complete affected packages GREEN: `go test -p 1 -timeout=30m ./internal/storage ./internal/api` → storage **441.305s** and API **19.591s**. A separate fresh-PG focused run of all nine new prequote tests (including foreign app-actor read denial added after the broad run began) passed in **8.558s**.
- **Script checks:** `python3 -m unittest discover -s scripts -p 'test_ci_*.py'` passed **31**, `test_factory_efficiency.py` passed **17**, and `test_factory_workflow_contract.py` passed **13**; their temporary Git fixture did not alter the #831 worktree. `gentle-ai review mode status` reports clone-local **off** (global on), so no RDD review was started or inferred.
- **T1 work-unit commit:** `f8417a7c01b9b1ff733870dd26f02e9eae6247dd` (`fix(digital-thread): prepare draft units on Design intent`), 10 backend/contract/generated/test files, **692 additions + 4 deletions**; no AI attribution. Its exact identity was recorded with the T2 task-document commit.
- **T2 work-unit commit:** `e4befb6e8635fbe03a149beba349e55e6635862f` (`feat(ui): prepare existing Design units before SketchUp handoff`), 4 UI/browser/task-document files, **411 additions + 18 deletions**. The two source commits retain all tests and the single coherent PR shape; this evidence update records the second SHA after its commit.
- **T3 local verification complete, publication pending:** The new Go Q1 test and real Chromium Q1 action each reuse the prepared FI/link IDs. Full affected storage/API packages, focused React 58/58, TypeScript typecheck, three script suites and generated API drift passed after final source normalization; the earlier full `pnpm test` pass was **before** the last minor React handler normalization, so it is supporting, not exact-final-byte proof. `python3 scripts/factory_preflight.py --require-clean` on `e4befb6e` returned `PREFLIGHT_OK_NOT_VERIFIED` with `dirty=false`; it runs no tests and is not functional proof. T3 stays unchecked until independent exact-head review and current CI after the parent publishes the candidate. No merge or autoapproval is authorized.
- **Scope and delivery:** No product write outside #831; no Q/status shortcut, autoapproval, merge, push, PR or host operation. #642 is not required for this validated UI Q1 path. The generated client is one line. `verify_affected.py --base origin/main --plan` conservatively selected TypeScript, backend-Go, SketchUp, visual, foundation-PostgreSQL and organization-browser due shared/backend paths; the scoped organization browser gate and affected checks above ran, whereas unrelated physical SketchUp and visual/foundation-wide gates are **NOT_RUN**, not PASS. Current #398 host/Q/R/Engineering continuation remains **NOT_RUN**.

Next: parent performs independent exact-head review, then publication/current CI under ordinary repository policy; retain T3 unchecked until that evidence exists. #398 real SketchUp-first, later Q/R and exact Engineering release remain separately **NOT_RUN**. No installation, host file, Keychain or persistent database was touched.

Engram mirror: `odd/831-prequote-draft-units/tasks`, observation #1631; full document saved and read back on 2026-09-23. The repository file remains authoritative.
