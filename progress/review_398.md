# Review — F211 / #398 (DT-14) End-to-End Digital Thread Contract & Regression Gate

**Fecha:** 2026-09-04 · **Rama:** `feat/398-digital-thread-e2e-regression-gate` · **Commit:** `bea571c1` (pushed, PR #554 OPEN)

**Veredicto:** CHANGES_REQUESTED

**Resumen:** el suite Go E2E (9/9 verde contra PostgreSQL real con rol app `granete_app_test` NOBYPASSRLS), el unit contract Ruby (verde), el wiring CI (vía mecanismo canónico existente) y el ledger están bien construidos y ejecutados. Pero el smoke TestUp del host real **no es ejecutable tal como está escrito** (`Library::FurnitureBuilder` no existe; es `Model::FurnitureBuilder`, y además se instancia sin `metadata_store:`), no hay evidencia de ejecución host, y hay assertions más débiles que su propio contrato (Scenario F) y documentación que no cumple un criterio de aceptación explícito de la issue (guía de fixtures/sin snapshot-blessing). Ninguno de estos puntos invalida el trabajo de fondo; todos son corregibles sin tocar código de producción.

---

## Checkpoints (protocolo skill)

- C1: [x] Harness completo (`AGENTS.md`, `init.sh`, `feature_list.json`, `progress/current.md`, `CHECKPOINTS.md`, 4 docs, 3 skills presentes). No ejecuté `./init.sh` completo; ejecuté sus gates relevantes por área (ver "Capas verificadas").
- C2: [x] Sólo `F202` en `in_progress` (permitido, programa P0 paralelo); F211 `done` con tests pasando (`go test ./...` verde, `pnpm test` 411/411). `progress/current.md` describe la sesión activa F211.
- C3: [x] Sin cambios en `packages/*`; el diff no toca código de producción en absoluto (solo tests/contratos/docs/ledger — correcto para un regression gate). Identificadores técnicos en inglés, comentarios de invariantes claros.
- C4: [x] Verificación real: suite E2E ejecutada contra PostgreSQL 16 real (docker `muebles-postgres:5445`) con rol app, RLS y transacciones tenant reales. Golden/fingerprint determinista verificado (sort estructural por `FurnitureInstanceID` en `domain/production_release.go:102`).
- C5: [ ] `progress/history.md` NO recibió entrada por esta sesión (última entrada es de F199). Coincide con la práctica reciente de F203–F210 (ninguna la actualizó), pero el checkpoint canónico pide la entrada. Menor.

## #398 E2E gate — checklist específico

### Escenarios canónicos

- [x] **A Quote-first** — `digital_thread_e2e_test.go:23-391`. Dos líneas → FI-001 ≠ FI-002 (l.73-78), QuoteLine ID ≠ identidad física (l.76-78), Design creado desde `quoteRevQ1` exacta (l.115), working copy con las MISMAS identidades (l.138-153), width 650 sólo en FI-002 (l.160-182), R1 real (l.184-195), reconcile synced=1/modified=1 (l.209-212), clasificación `domain.ClassifyReconciliation` con `RequiresRequote` (l.214-220, no hardcodeada), requote explícito Q2 draft rev 2 (l.222-234), lifecycle draft→published→accepted (l.237-248), reconcile Q2↔R1 non-blocking (l.251-262), Q1 byte-idéntico tras requote (l.270-276), approval con `ApprovedBy`/`ApprovedAt` server-authoritative (l.282-292), release P1 pinea R1+Q2+fingerprint sha256 (l.294-311), R2 post-release con P1 byte-idéntico (serialización antes/después, l.319, 363-369) + readback con pins intactos y `ManufacturingStale=true` (l.372-390).
  - [ ] Sub-item débil: no se asserta **snapshot equivalente** del approval (sólo metadatos ApprovedBy/ApprovedAt); y el publish E2E usa el path directo `PublishDesignRevision` (snapshot de working copy) sin ejercitar el path de **artefactos** (`.skp`+manifest+preview vía `FinalizeDesignPublish` de #392). La inmutabilidad de artefactos queda cubierta por el suite #392, pero el gate no lo dice en ningún lado (ver cambio requerido 4).
- [x] **B Quantity>1** — l.396-502. qty=3 → 3 FI distintas (l.418-429), QuoteLine ID ≠ identidad (l.430-432), diseño con 2 → tercero `quoted_not_modeled` por FI exacto (l.481-495).
- [x] **C Design-first** — l.507-633. FI-D `origin: design` (l.550-562), `modeled_not_quoted` antes de requote (l.599-601), requote incorpora el MISMO FI-D (l.629-632).
  - [ ] Sub-item débil: "nunca crea FI-E" sólo se prueba por presencia de FI-D; no se asserta que el conteo de FurnitureInstances del project sea igual antes/después del requote (ver cambio requerido 3).
- [x] **D Duplicate** — l.638-722. Working copy con identidad duplicada → fail closed (l.663-674); resolución crea FI-NEW `origin=duplicate` + `originFurnitureInstanceId` preservado (l.677-690); publish limpio tras resolver (l.693-715). La detección de duplicado en host (copy/paste real) está en Ruby unit (l.229-278 del test Ruby) — correcta división de capas.
- [x] **E Semantic scope** — Go l.727-815 (sólo managed items llegan a `design_revision_items`, conteo verificado con admin, l.809-814) + Ruby unit `test_scenario_e_unmanaged_geometry_exclusion` (group/def sin metadata excluidos del manifest) + TestUp smoke (face + group "Unmanaged Wall"). La prueba Go es estructuralmente débil (sólo prueba que lo que no se envía no aparece); la exclusión real vive en Ruby/manifest — aceptable porque el smoke la cubriría en host, pero hoy el smoke no corre (ver B1).
- [ ] **F Concurrency** — l.820-910. El publish stale base es rechazado (errConflict ≠ nil, l.898-900), PERO: (a) el check de tipo de error en l.901-903 es un no-op (`t.Logf` en la rama de error inesperado, nunca falla); (b) NO se asserta que la revisión actual queda intacta ni que **no se creó R3/artefacto huérfano**, que es lo que el propio contrato (`contracts/digitalThreadE2E.json:80`) y el ledger (acceptance F211) prometen ("creates no revision or orphaned artifacts"). Ver cambio requerido 2.
- [x] **G Release durability** — l.915-1004. P1 contra R3/F3; tras R4/R5 el readback sigue pineado a R3/F3 exactos (l.984-997). NOTA: el doc (`docs/architecture/digital-thread-e2e-regression-gate.md:114`) afirma que el trigger `protect_production_release_immutability` bloquea UPDATE/DELETE "at SQL level" como paso 4 del scenario G — ese proof vive en #395 (`TestProductionRelease_ReleaseRowsAreImmutableHistory`), no en este gate; el gate sólo hace la prueba positiva de R2 (Scenario A) y G (R4/R5).

### Pruebas negativas obligatorias

- [x] Identidad: unidades visualmente idénticas siguen siendo unidades distintas (qty=3, y comando idéntico → IDs distintos viene de #385); todos los joins del gate son por `FurnitureInstance.id` explícito (reconcile/release/working copy).
- [x] Fake UUID sintácticamente válido desconocido → reconcile fail closed (l.1081-1092).
- [ ] Revisiones exactas / estabilidad de comparación: el gate NO re-ejecuta reconcile(Q1,R1) tras existir Q2/R2 para probar que el resultado histórico no cambia. Cubierto en el suite #393 (`TestReconciliation_HistoricalQuote_OldQuoteRevisionStaysOld`), pero el ledger F211 lo atribuye a este gate. Ver cambio requerido 5.
- [x] Inmutabilidad: Q1 accepted byte-idéntico tras requote (l.270-276) + UPDATE directo por SQL sobre quote aceptado bloqueado por trigger (l.1136-1139); P1 byte-idéntico tras R2 (l.363-369). Published/approved DesignRevision a nivel DB: cubierto por suites #392/#395, no re-probado aquí (consume, OK).
- [ ] Idempotency: el gate NO replaya ninguna key (create/duplicate/publish/requote/approve/release con misma key). Existe cobertura en suites enfocados (#385 convergencia materialize, #392 finalize idempotente por sesión, #395 `TestProductionRelease_ApprovalLifecycleAndIdempotency`), lo cual es consistente con "consume, not duplicate" del comentario de #398 — pero el ledger F211 lista "idempotency" como negative proof de ESTE gate. Ver cambio requerido 5.
- [x] Cross-project: reconcile A↔B rechazado (l.1059-1075) y release pineando revisión de otro project rechazado (l.1066-1074).
- [x] Cross-org/RLS: Org B no puede crear Design en project de Org A (l.1094-1108) — vía `WithinTenantTx` con rol app real `granete_app_test` (NOBYPASSRLS, `tenant_rls_test.go:64-66`), no handler RBAC.

### Reglas de calidad del suite

- [x] Ejercita caminos reales de producción: todos los pasos llaman métodos del `PostgresStore` real (los mismos que usan los handlers HTTP) dentro de tenant tx con rol app; nada reconstruye lógica ni arma resultados a mano (las únicas escrituras directas son seeds de `project_items`, patrón estándar del package).
- [x] Capas separadas y reportables: Go storage/RLS, Ruby unit (SketchupStub), TestUp host, contrato JSON. El reporte de estado por capa NO existe como tabla honesta (host debiera decir REAL_HOST_REQUIRED/no ejecutado) — ver cambio requerido 4.
- [ ] Host real: **FALLA**. `TC_DigitalThreadE2ESmoke.rb:150` referencia `Library::FurnitureBuilder`, que no existe (la clase es `Granete::SketchUpExtension::Model::FurnitureBuilder`; ver `TC_DesignPublishSmoke.rb:181` y `TC_ProjectFurnitureSmoke.rb:171`). Reproduje la resolución: `NameError: uninitialized constant Granete::SketchUpExtension::Library::FurnitureBuilder`. Además `.new` sin `metadata_store:` implica que `MetadataWriter.write_furniture` retorna temprano (`model/furniture_builder.rb:730-737`: `return unless store`) y no estampa metadata Granete, con lo que `ManagedFurniture.locate`/`ManifestBuilder` no encontrarían nada y TODAS las assertions del smoke fallarían. Los 3 tests del smoke usan `builder`. Además no existe artefacto de evidencia host (convención del repo: `progress/host_smoke_F*_testup_ci.json` con TestUp::CIJsonReporter contra RBZ instalado).
- [x] Gate mode / fail-closed sin infra: el suite hereda `t.Skipf("no db")` del fixture compartido (`multi_org_migration_test.go:63-67`) — convención preexistente del package storage, NO introducida por este diff, y mitigada en CI porque `ci.yml:161-165` fija `DATABASE_URL` con comentario explícito anti-verde-falso (OC-002). Aceptable; el doc debería mencionar que sin `DATABASE_URL` el gate se salta localmente.
- [x] Fixtures deterministas: UUIDs explícitos estables; IDs generados por el server se capturan y comparan explícitamente (`fi001/fi002`, `f1`, `revR1ID`); fingerprint ordena por identity antes de hashear (`production_release.go:120-123`).
- [x] Sin snapshot blessing: no hay goldens binarios nuevos que bendecir; las comparaciones son de valores semánticos (estados, pins, conteos), no blobs opacos.
- [x] Fingerprint determinista: mismo input → mismo hash; transform espacial no altera (l.1239-1242); cambio manufacturero (width) altera (l.1281-1284). Independencia de orden es estructural (sort) pero no tiene test explícito de permutación — menor.
- [x] Mensajes de fallo nombran el contrato roto (p.ej. l.367 "invariant violation I7: ProductionRelease row mutated...", l.427 "invariant I1 violated..."). Excepción: la guarda debilitada de Scenario F (l.901-903).
- [x] Sin feature nueva: el diff NO toca código fuente de producción (diffstat: sólo 3 tests + contrato + doc + ledger). Correcto: #398 es un gate de regresión, no una feature. Nada de #496-#502/#397/#499/hardware/Blum/DXF/SCM/machine profiles/ProductionRelease behavior nuevo.
- [x] CI wiring: el gate corre por el mecanismo canónico existente — job `backend-go` de `ci.yml` corre `go test -p 1 -v ./...` con service container postgres:16 + `DATABASE_URL` obligatorio (el suite E2E es parte de `./internal/storage`), y job `sketchup-extension` corre `bundle exec rake verify` en 3 OS (incluye el unit contract nuevo). No requería cambios de workflow. Host smoke queda como gate manual licenciado (documentado en `ci.yml:120-123`).
- [ ] Docs: explica qué protege, escenarios y cómo correr (`docs/architecture/digital-thread-e2e-regression-gate.md`), PERO falta: cómo agregar/actualizar fixtures sin snapshot-blessing (criterio de aceptación explícito de la issue #398), revisión de goldens, y el estado honesto de capas (host = REAL_HOST_REQUIRED / no ejecutado; artifact finalize consumido de #392). Ver cambio requerido 4.
- [x] Ledger: `feature_list.json` F211 `done` con depends_on F210 (`done`), sólo F202 `in_progress`; `progress/current.md` y `progress/implementation_398_dt14.md` consistentes en rama/commit/comandos. Salvo sobre-claim de coverage (ver cambio requerido 5).

## Cambios requeridos

1. **[Blocker] Corregir el smoke TestUp para que sea ejecutable** — `apps/sketchup-extension/test/testup/TC_DigitalThreadE2ESmoke.rb:150`: reemplazar `Library::FurnitureBuilder.new` por `Model::FurnitureBuilder.new(metadata_store: metadata_store)` (mismo patrón que `TC_DesignPublishSmoke.rb:181`). Sin `metadata_store:` el placement no estampa metadata Granete (`model/furniture_builder.rb:737`) y las 3 assertions del smoke son infalsables. Luego ejecutar la suite en SketchUp real y registrar la evidencia según convención (`progress/host_smoke_F211_testup_ci.json` estilo TestUp::CIJsonReporter contra RBZ instalado). La capa host debe reportarse `REAL_HOST_REQUIRED` hasta que exista esa evidencia — hoy el implementador no la ejecutó ni la reportó como pendiente.
2. **Scenario F: assertions a la altura del contrato** — `backend-go/internal/storage/digital_thread_e2e_test.go:898-903`: (a) exigir `errors.Is(errConflict, domain.ErrDesignRevisionConflict)` (o el error tipado exacto que emite `PublishDesignRevision`) y fallar si no; (b) tras el rechazo, assertar que el head sigue en R2 y que **no existe R3** (conteo de revisions del design == 2) ni artefactos finalizados del intento fallido — hoy sólo se prueba "error ≠ nil" y el branch l.901-903 es un no-op que loguea en vez de fallar.
3. **Scenario C: probar que no se crea FI-E** — `digital_thread_e2e_test.go:628-632`: además de verificar presencia de FI-D en el draft, contar `furniture_instances` del project antes/después del requote y assertar invariante (mismo conjunto de IDs, sin altas). Eso convierte "nunca crea FI-E" de implícito a probado.
4. **Docs del gate: completar lo que la issue exige** — `docs/architecture/digital-thread-e2e-regression-gate.md`: añadir (a) sección "cómo agregar/actualizar fixtures sin snapshot-blessing" (criterio de aceptación explícito de #398, hoy ausente); (b) tabla de estado por capa con el host marcado `REAL_HOST_REQUIRED` (sin ejecutar hasta cambio 1); (c) nota de boundary: el publish E2E usa el path directo de snapshot (los artefactos `.skp`/manifest/preview y su inmutabilidad son consume del suite #392 `FinalizeDesignPublish`); (d) corregir el paso 4 de Scenario G (l.114): el trigger `protect_production_release_immutability` se prueba en #395, no en este gate; (e) aclarar que sin `DATABASE_URL` el suite local se salta (CI lo garantiza).
5. **Ledger/notas: atribución honesta de coverage** — `feature_list.json` (entrada F211, campo `description`/`evidence`) y `progress/implementation_398_dt14.md` §B.4/§B.7: idempotency, estabilidad de comparación histórica (reconcile(Q1,R1) invariante ante Q2/R2) y "no join by name/geometry" se consumen de los suites enfocados (#385/#392/#393/#395), no están en el archivo E2E; el reporte del implementador además describe Scenario D como "Host duplicate detected on preflight" y Scenario G como "DB triggers block direct SQL mutation or deletion" — ninguno de los dos ocurre en el test entregado (D falla en `UpdateDesignWorkingCopy`; G no intenta SQL directo). Reformular para que el gate no reclame proofs que no contiene (o moverlos al gate, ver 2/3).
6. **[Menor] Consistencia de contract/headers** — (a) `apps/sketchup-extension/test/unit/digital_thread_contract_test.rb:23` promete "Scenario C (Design-first): Catalog placement identity" pero no existe `test_scenario_c_*` en el archivo (la cobertura de inserción por catálogo vive en `project_furniture_test.rb` de #389): quitar la línea o agregar el test; (b) `contracts/digitalThreadE2E.json:88-98` renumera invariantes I1–I9 con significados distintos a las I1–I14 normativas de `project-design-digital-thread.md` §3 (p.ej. contract I5 ≠ spec I5): referenciar el numbering canónico o prefijar (p.ej. `C1..C9`) para no invitar a confusión.
7. **[Menor] `progress/history.md`**: agregar entrada de cierre de sesión F211 (checkpoint C5; omitido también por F203–F210, pero el canónico la pide).

## Capas verificadas

| Capa | Estado | Evidencia |
|---|---|---|
| Go storage E2E (PostgreSQL 16 real, rol app `granete_app_test` NOBYPASSRLS, RLS, tenant tx) | **EJECUTADA, VERDE** | `go test -p 1 -count=1 -run TestDigitalThreadE2E ./internal/storage` → 9/9 PASS (ScenarioA/B/C/D/E/F/G, NegativeProofs, DeterministicFingerprintParity) contra `localhost:5445` (docker `muebles-postgres`) |
| Go suite completa | **EJECUTADA, VERDE** | `go test -p 1 -count=1 ./...` → exit 0 (storage 216s, pilotreadiness 236s, api/domain/auth ok) |
| TypeScript monorepo | **EJECUTADA, VERDE** | `pnpm typecheck` ok; `pnpm test` 411/411 (33 files); `pnpm openapi:check` sin drift |
| Ruby unit contract (SketchupStub) | **EJECUTADA, VERDE** | `ruby -Itest -Isrc test/unit/digital_thread_contract_test.rb` → 7 runs, 38 assertions, 0 failures/0 skips (bundler local sin gems; corrido sin bundler). `rake verify` completo (rubocop/RBZ) NO ejecutable en este entorno — CI lo corre en 3 OS |
| TestUp host real (SketchUp) | **NO EJECUTADA + NO EJECUTABLE** | Sin artefacto `host_smoke_*` para F211; `TC_DigitalThreadE2ESmoke.rb:150` reproduce `NameError ... Library::FurnitureBuilder` (verificado con probe de resolución de constantes). Debe reportarse `REAL_HOST_REQUIRED` tras fix |
| Git/ledger | **VERIFICADA** | Working tree clean; `git log origin/feat/398...HEAD` vacío (todo pushed); `git diff --check` limpio; PR #554 OPEN; diff = tests/contratos/docs/ledger, sin código de producción |
