# Closeout — F007 ui_option_groups

**Date:** 2026-07-15  
**Status:** closed (`done`)

F007 (UI de OptionGroups en `@muebles/ui`: helpers puros `membersForKind` / code uniqueness / `canShowPricePreview`, `OptionGroupsScreen` CRUD controlado, multi-select de miembros filtrado por kind, `PricePreviewGate` + demo OPT-05 en shell web con `calcProjectBreakdown` solo en app) was self-verified green (`pnpm --filter @muebles/ui test` 27/27, `pnpm --filter @muebles/web test` 7/7, typecheck ui+web, Vite web build, monorepo `./init.sh` + `pnpm test`), reviewed with verdict **APPROVED** in `progress/review_F007.md` (acceptance OPT-01/02/03/05 and C1–C4 pass; OPT-04 N/A; no required changes; residual notes non-blocking), and harness-closed: `feature_list.json` status set to `done`, session summary appended to `progress/history.md`, and `progress/current.md` reset to idle with next feature F008 (`ui_module_editor`) still pending and not started.
