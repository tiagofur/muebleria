# Fragmentos semánticos UI/Proyectar

F100–F159 candidate fragment review; omitted IDs handled by main feature worker; no product edits/tests/DB execution.

| ID | Actual comprobado en fuente | Límite |
|---|---|---|
| F100 | AppShell derives area from navigation sections; CSS maps each area to generic canvas/chrome/container/state roles. | Full area matrix visual calibration, all descendants and portal escape contexts not runtime verified. |
| F101 | Header names primary/secondary/overflow slots and renders destructive overflow through menu; toolbar supplies search/filter/tab slots. | primaryAction accepts arbitrary ReactNode: one-primary is convention, not enforced number. All consumers and keyboard overflow not certified. |
| F102 | Shared implementation exposes workspace/workflow variants, excludes disabled tabs from roving IDs and associates tabs with panel IDs. | Consumers must actually provide panels and semantics; responsive overflow and all keyboard paths not exercised. |
| F107 | Area families consume separate canvas/chrome tokens and explicit selected/hover/pressed/focus roles. | Token routing alone does not prove distinguishable live tints or side-by-side calibration. |
| F108 | Readable muted text has literal HSL token; inspected tests calculate luminance contrast against selected surfaces and semantic badge pairs. | Test source is not rerun here; computed browser overrides/transparency across screens not measured. |
| F109 | Agregado editor consumes WorkspaceTabs; rollout guard scans ui TSX/CSS for competing tab markup and legacy classes. | Source-text guard is not semantic classification of every workspace or proof of keyboard behavior. |
| F112 | libreria/catalogos sections map to library; engineering remains eng and unknown section falls back neutral. | All nine nav membership entries and live context after each navigation not independently exercised here. |
| F116 | Material creation and update calculate cost and preserve preview/PBR fields through shared normalization plus patch persistence chain. | Local/API refresh persistence and failure/race cases for all catalog fixes remain unexecuted; do not infer save success from field presence. |
| F117 | Material catalog imports extracted form/helpers; catalogStore is132lines. Current MaterialsCatalog is694lines. | Original acceptance threshold MaterialsCatalog<450 is not met at audited SHA; decomposition exists but no COMPLETE claim. Other file thresholds/test parity need verification. |
| F126 | Initial strategy prefers saved plan then workshop default then saw; nesting branch offers distinct sheets/pieces DXF actions disabled without plan/callback. | Saw branch/full export dispatch outcomes and physical machine import not executed; DXF known defects retained separately. |
| F143 | Selection distribute/align/compact handlers check canEdit and call domain commands with intent labels rather than calculating geometry locally. | Multi-selection collision/all-or-nothing outcomes and all axis/keyboard cases need command tests and real viewport exercise. |
| F144 | Keyboard guard yields to input/modal/tablist; undo/redo shortcuts and selected nudge dispatch carry precision step and coalescing intent. | Root guest placement undo/redo only corroborates narrow history use, not collision-free nudge, dimension commits, all widgets or frozen-plan handling. |
| F142 | Material dock selects board palette versus ambient palette with explicit scope/canEdit and floor/wall/ceiling/countertop IDs. | Actual material drop scope, BOM/price propagation, undo and reload/API persistence not verified by viewing palette. |
| F148 | Benchmark declares11canonical tasks/targets, real versus proxy source, feature flag and localStorage persistence with fallback to memory. | No facilitated real-user session or achieved timing recorded; target constants never actual performance. |
| F149 | Delete distinguishes unplace (layout-only) from project-line removal; latter captures item snapshots/order in history and removes matching item IDs. | Multiple quantity instances, persistence failure, full restore/redo and accessible confirmation need exercise; source intent does not establish server atomicity. |
| F151 | Card actions hide opacity/pointer events at rest, reveal on hover/focus-within and stay visible on non-hover devices; reduced motion removes transition. | Every intended card consumer and actual keyboard/touch target sizing not certified by shared CSS. |
| F152 | Module selection uses canonical routable hook seeded by openModuleId and known memoized IDs. | Hook internals and real deep-link invalid-ID/loading/back/forward matrix require separate proof; no module F5 claim from quote reload. |
| F153 | Initial selectedId is URL-derived; plan callbacks depend on selectedProjectId instead of full object. | Root guest quote reload stays quoted/priced; not exhaustive render-loop regression across empty/loading/auth transitions. |
| F154 | Expandable rows expose aria-expanded, ChevronRight and keyboard Enter/Space; nested controls avoid row-key handling and action containers stop propagation. | Rotation CSS and all6catalog consumers/real keyboard screenreader behavior need further verification. |
| F155 | Structure destructive/admin moreItems gated by canMutate and callback availability; separate deactivate/reactivate based on active state. | Actual menu rendering, confirm flow, permissions and destructive backend failure not exercised by this fragment. |
| F156 | Missing/unsafe src gives aria-hidden decorative placeholder without role/name; real img retains alt. | All ancestor accessible names and all URL/security cases not certified; source test asserts decorative props and valid image alt. |
| F157 | Scope projects items/walls/placements/spaces to selected ambient while all returns project; unknown space preserves project. | UI caller selection and all-space grouped3D/planta, unplaced items and backend authorization are separate verification. |
| F103 | Design guide explicitly delegates paths to NAV_PATHS and role visibility to executable guard, labels progress historical rather than delivery authority. | This section is contrasted, not all4documents or all nav labels; newer documentation drift findings remain valid. |
| F104 | Materials uses PageHeader title/icon/subtitle and a single canMutate-gated create action. | One catalog reviewed here, not all9consumers; toolbar/search/menu and alternate empty/error render paths not certified. |
| F105 | Plant board page header declares read-only ownership and empty state directs user to accepted quotes. | Dashboard/Sales/Customers/Showcase full rollout and all states not inferred from this consumer; root guest plant board is narrow corroboration. |
| F106 | Settings uses shared PageHeader and WorkspaceTabs; salesNetwork controls whether network tab is included. | All production/configuration screens and removal of old CSS vocabularies not fully checked in this fragment. |
| F111 | Shared stat-card and semantic status-badge foundations exist; LifecyclePanel still uses badge--success-subtle/badge--neutral-subtle. | Single-vocabulary acceptance is not demonstrated; legacy/local badge family still appears in this caller. No commercial severity from vocabulary alone. |
| F115 | Generate clamps kerf/trim, delegates optimizeCutPlan and stores local plan; Save calls callback then immediately sets success. | Pure optimizer guarantees/persistence not proved by this caller. Save feedback precedes authoritative result; consolidate with existing false-success class, not separate data-loss claim. |
| F131 | Mounted gizmo defines move/rotate callbacks but neither is called by its rendered handles; pointerDown/up only change activeHandle and color, with no movement event handler. | CONFIRMED static nonfunctional drag/rotate wiring for this gizmo; real viewport not executed. Manual fields and other editors may work and are not declared broken. |
| F132 | SCM native postprocessor is explicitly postponed pending exact machine model/software; ledger pending aligns with an unready capability. | No machine/software confirmation or native output/readback. DXF import promise in ledger is not homologation evidence; known DXF defects bound the fallback claim. |
| F139 | Closeout readiness evaluates all units installed, open field issues, blocking punch items, visits, optional signed conformity. | Pure predicates do not prove server gate or workflow persistence; actual closeout/signoff/warranty chain requires routed API and forbidden-transition evidence. |
| F140 | Operational stage, commercial status and production release are distinct displayed concerns; release dialog entry reports gate counts. | Visible6gates must match domain and authoritative enforcement; no claim UI enforces release or generic-write hazards fixed. |
| F141 | Library availability requires editing plus catalog insert callback; click insert returns item ID then places on active wall or selects unplaced item. | Root existing-item placement does not prove catalog drag insert, taxonomy/favorites, all constraints or save/reload/API insertion. |
| F145 | Wall add/update/remove delegate domain environment commands; removing wall reports furniture now unplaced and commits intent. | Multiple-space add/switch cloning, invalid dimensions, overlapping openings, undo and save/reload not executed. |
| F146 | TS fixture test compares exact BOM part signatures/material multiplicity and hardware totals plus price breakdown; Go compares hardware and same expected costs. | Source test semantics confirmed, not this subtask execution. Fixture coverage is not universal model/material compatibility or release staleness proof. |
| F147 | Perf reference seed is opt-in and test requires deterministic scene,20–30active placements, two spaces and hundreds of pieces/hardware. | Test fixture/telemetry presence gives no actual FPS or latency; representative browser profile and stored baseline still needed. |
| F150 | Order card title becomes a real button only with onOpenOrder; stretched hit area fills card while sibling interactive controls sit above. | Installation/shipment consumers and mouse/keyboard nested actions not exercised; source control structure not accessibility certification. |
| F158 | Separate island sheets render ProductionIslandPreview for free placements and explicitly do not project islands into wall elevations. | Actual dimensions/position/rotation accuracy, PDF mirrors and multi-island runtime still require fixture/export comparison. |
| F159 | Grouped wall/island sections key by spaceId and label spaceName; island child suppresses duplicate space label. | Grouping predicate and whole/all-versus-single-space selection paths, two-room UI/PDF no-exclusion validation not executed. |
