import json,re,pathlib,datetime,subprocess
root=pathlib.Path(__file__).resolve().parents[2]; src=root/'source'; out=root/'audit'
b=json.loads((out/'data/backend-audit.json').read_text()); deep=json.loads((out/'data/endpoint-deep-audit.json').read_text()); spec=json.loads(subprocess.check_output(['ruby','-ryaml','-rjson','-e','puts JSON.generate(YAML.load_file(ARGV[0]))',str(src/'contracts/openapi/granete-api.v1.yaml')],text=True))
done={(x['method'],x['path']) for x in deep['endpoints']}; notes={}; alias_map={
'/api/projects/{projectId}/furniture-instances/{instanceId}:duplicate':'HandleFurnitureInstanceDuplicate',
'/api/projects/{projectId}/quote-lines/{quoteLineId}:materialize':'HandleQuoteLineMaterialize',
'/api/furniture-instances/{instanceId}:remove':'HandleFurnitureInstanceRemove',
'/api/designs/{designId}/publish/{sessionId}:finalize':'HandleDesignPublishFinalize',
'/api/designs/{designId}/revisions/{revisionId}/artifacts/{kind}:authorize':'HandleDesignRevisionArtifactAuthorize'}
def note(names,purpose,auth,validation,effects,refs,tests,domain):
 for n in names.split(','):notes[n]=dict(purpose=purpose,authAndRoles=auth,validation=validation,sideEffects=effects,evidence=refs,tests=tests,domain=domain)
note('HandleListMySessions,HandleRevokeMySession','Directorio y revocación de sesiones propias','AuthMiddleware; identidad claims.UserID; revoke lleva RequireIdempotency; no-store y rechazo token query','SessionId del path; comando propio usa actorUserID, sin aceptar owner del cliente. Not found se mapea a SESSION_NOT_FOUND.','List es lectura; revoke delega revocación al storage con actor/IP/requestID. Metadatos no incluyen refresh secret.', ['backend-go/internal/api/session_directory.go:62-83','backend-go/internal/api/routes.go:169-170'],['backend-go/internal/api/session_directory_test.go:32'],'Identity / session registry')
note('HandleListMembershipSessions,HandleRevokeMembershipSession','Sesiones de una membresía del taller','TeamCapabilityRevokeSessions; target se busca en ListOrgTeam de claims.OrgID; soporte no puede mutar Team','Revoke exige body generado reason trim de1–200runas y membershipId existente del taller; revoke idempotente en ruta.','Lectura o revocación exacta con target user/membership/organization; no revocación global implícita.', ['backend-go/internal/api/session_directory.go:86-178','backend-go/internal/api/routes.go:212-213'],['backend-go/internal/api/session_directory_test.go:91'],'Organization Access')
note('HandleListPlatformUserSessions,HandleRevokePlatformUserSession','Sesiones globales de una cuenta','PlatformAdminMiddleware + sesión registrada; no-store/reject-query-token','Revocar exige reason1–200; userId/sessionId en comando. Error SESSION_NOT_FOUND404.','Solo revoke modifica; comando incluye actor y target distintos.', ['backend-go/internal/api/session_directory.go:169-213','backend-go/internal/api/routes.go:183-184'],['backend-go/internal/api/session_directory_test.go:131'],'Identity / platform')
note('HandleDeviceEnroll','Iniciar enrolamiento SketchUp','Anónimo con auth rate limiter y no-store','client_type exactamente sketchup; display_name trim1–120runas; JSON limitado.','Crea enrollment UUID, código aleatorio6símbolos, TTL10min; no credencial de usuario todavía.', ['backend-go/internal/api/devices.go:26-90'],['backend-go/internal/api/devices_test.go:57'],'Identity / device enrollment')
note('HandleDeviceEnrollPoll','Consultar estado enrolamiento','Anónimo con rate limiter/no-store; UUID de enrollment es entrada','UUID parse; inexistente404; pending vencido se proyecta expired.','Lectura de estado, no token.', ['backend-go/internal/api/devices.go:101-133'],['backend-go/internal/api/devices_test.go'],'Identity / device enrollment')
note('HandleDeviceApprove','Vincular código a usuario autenticado','AuthMiddleware + device_enrollment step-up ANTES de RequireIdempotency','Normaliza separadores/casing y exige6caracteres; claims.UserID obligatorio. Storage conflict409.','Aprueba enrollment para identidad del actor.', ['backend-go/internal/api/devices.go:181-226','backend-go/internal/api/routes.go:145'],['backend-go/internal/api/devices_test.go:22','backend-go/internal/api/devices_test.go:43'],'Identity / device enrollment')
note('HandleDeviceExchange','Intercambiar enrollment aprobado por secret','Anónimo rate-limited/no-store; enrollment_id UUID','UUID válido; almacenamiento rechaza no intercambiable409 o inexistente404.','Llama ExchangeAuthDeviceEnrollment; devuelve RawSecret solo en respuesta. Atomicidad de almacenamiento no reinspeccionada aquí.', ['backend-go/internal/api/devices.go:143-174'],['backend-go/internal/api/devices_test.go'],'Identity / device enrollment')
note('HandleDeviceToken','Emitir bearer SketchUp desde secreto de dispositivo','Credencial de dispositivo, no web JWT; rate limiter/no-store','Parser exige separador y secret>=64caracteres; ResolveDeviceToken decide credencial; missing/revoked401uniforme.','Token lleva sesión, organización, membership y credential versions; IssueTransportTokenUntil limita expiración al absolute bound.', ['backend-go/internal/api/devices.go:250-326'],['backend-go/internal/api/devices_test.go:89','backend-go/internal/api/devices_test.go:159'],'Identity / token authority')
note('HandleListMyDevices,HandleRevokeDevice','Directorio y baja de dispositivos propios','AuthMiddleware; claims.UserID; no-store; revoke idempotente','Revoke valida device_id UUID y OwnerUser en storage; missing404.','Listado proyecta metadatos sin hash; revoke comando OwnerUser/deviceID, requestID/IP.', ['backend-go/internal/api/devices.go:331-379','backend-go/internal/api/routes.go:148-149'],['backend-go/internal/api/devices_test.go'],'Identity / devices')
note('HandleOrgTeam,HandleOrgTeamSummary,HandleOrgTeamMember','Leer equipo por membresía, no cuentas globales','TeamCapabilityView según roles y tipoorg; requiere org activa; soporte puede consultar','Target individual se busca dentro del equipo de claims.OrgID; missing MEMBERSHIP_NOT_FOUND.','Lectura; directory summary incluye capacidades, límites/versiones; member ETag versión. AccountStatus y MembershipStatus separados.', ['backend-go/internal/api/orgteam.go:35-54','backend-go/internal/api/orgteam.go:150-247'],['backend-go/internal/api/orgteam_capabilities_test.go:227','backend-go/internal/api/orgteam_capabilities_test.go:501'],'Organization Access / Team')
note('HandleOrgMemberRoles','Modificar roles de membresía vía ruta legacy','Team view + target manageable; no soporte ni autoedición; assign-admin capability; org_admin step-up/idempotency','Body generado; RequireIfMatch; RolesAllowedInOrg; compara roles actuales y deseados con capacidades.','UpdateMembershipRolesByOrg + auditRequired + ETag; misma función que comando canónico. Transaction boundary completo no reinspeccionado aquí.', ['backend-go/internal/api/orgteam.go:249-306','backend-go/internal/api/routes.go:220'],['backend-go/internal/api/orgteam_capabilities_test.go:271'],'Organization Access / membership authority')
note('HandleOrgMemberStatus','Suspender/reactivar membresía legacy','Team mutation, target manageable, no soporte/autoedición; idempotency','If-Match; solo active/suspended; suspended exige reason; offboard no permitido por este campo.','UpdateMembershipStatus + auditRequired. Credenciales/last-admin dependen de storage ya cubierto en foundation, no prueba repetida.', ['backend-go/internal/api/orgteam.go:308-360','backend-go/internal/api/routes.go:221'],['backend-go/internal/api/orgteam_capabilities_test.go:307','backend-go/internal/api/orgteam_capabilities_test.go:336'],'Organization Access / membership lifecycle')
note('HandlePlatformListOrganizations,HandlePlatformUsers,HandlePlatformOrgAudit','Lectura administrativa de organizaciones/cuentas/auditoría','PlatformAdminMiddleware exige flag platform_admin después AuthMiddleware','Listaorg no paginada en handler; audit usa limit100 fijo; users carga memberships por usuario.','Lectura; errores de ListOrgTeam ignorados en conteo organización (puede mostrar0); ListMembershipsByUser sí propaga error.', ['backend-go/internal/api/platform.go:27-38','backend-go/internal/api/platform.go:87-101','backend-go/internal/api/platform.go:202-238'],['backend-go/internal/api/contract_448_test.go'],'Platform administration')
note('HandlePlatformUpdateOrganization','Actualizar nombre/licencia organización','PlatformAdminMiddleware + platform_admin step-up + RequireIdempotency','RequireIfMatch; solo name/license_plan/license_expires_at; nombre no vacío; planenum; fechaISO; VERSION_CONFLICT412.','UpdateOrganizationVersion, auditRequired de nombre/licencia, ETag. No inferir atomicidad solo del orden de funciones.', ['backend-go/internal/api/platform.go:104-199','backend-go/internal/api/routes.go:180'],['backend-go/internal/api/contract_448_test.go'],'Organizations / licensing')
note('HandleMediaUpload','Subir imagen de catálogo','AuthMiddleware; AnyRole RoleCanMutateCatalog (admin/ingeniero)','Multipart file, límite3MiB+overhead; MIME sniff con fallback extensión (BE-001); MediaDir requerido503.','Escribe archivo exclusivo en directorio org derivado de contexto; devuelve url/filename201. No rollback de archivos parciales verificado.', ['backend-go/internal/api/media.go:21-130'],['backend-go/internal/api/media_test.go'],'Catalog media')
note('HandleMediaGet','Leer recurso multimedia exacto','Ruta con mediaGetAuth; grant o auth según middleware, no asumir pública','Nombre sin .. ni separadores; directorio org del contexto; missing404.','Lectura filesystem; Content-Type por extensión; cache private/vary Authorization, grant cap. Ver BE-001 para bytes inválidos aceptados en upload.', ['backend-go/internal/api/media.go:133-196','backend-go/internal/api/routes.go:555-565'],['backend-go/internal/api/media_test.go'],'Catalog media')
note('HandleMediaAuthorize','Emitir grants de lectura por recurso','AuthMiddleware + no-store; sesión y org del actor','Body generado resources1–100; nombres válidos; dedup; ausentes/crossorg omitidos; signer/dir503.','Grants exact resource/session/org, cap sesión; sin publicación global de archivos.', ['backend-go/internal/api/media_authorize.go:29-102'],['backend-go/internal/api/media_authorize_test.go'],'Identity / media grants')
note('HandlePurchaseOrders,HandlePurchaseOrderByID,HandlePurchaseOrderEmit,HandlePurchaseOrderCancel,HandlePurchaseOrderReceive','Ciclo OC: draft→emitida→recibida/cancelada','GET admin/gerente_produccion/almacen via RoleCanAccessPurchasingNav; writes admin/almacen via RoleCanManagePurchasing; AuthMiddleware','Create id/supplier obligatorios; items kind válido/material no vacío/qty>0; date formato; costo>=0; allocation project existente. PUT solo draft; emit/cancel/receive verifican estado; receive mapea algunos errores por substring.','Receive: tx + row FOR UPDATE, valida pertenencia y acumulado<=restante, escribe stock+receivedqty+estado, commit. No receipt/idempotency wrapper en ruta: lock evita exceso pero no equivale a deduplicar retry parcial.', ['backend-go/internal/api/purchaseOrders.go:42-363','backend-go/internal/storage/purchaseOrders.go:269-381','backend-go/internal/api/routes.go:509-515'],['backend-go/internal/api/purchaseOrders_test.go:143','backend-go/internal/api/purchaseOrders_test.go:214','backend-go/internal/api/purchaseOrders_test.go:338'],'Procurement / inventory')
note('HandleMaterials,HandleMaterialByID,HandleHardwares,HandleHardwareByID','CRUD catálogo materiales/herrajes','GET autenticado con redacción costos por actorCanViewCosts; write AnyRole RoleCanMutateCatalog (admin/ingeniero)','JSON dominio; material create exige manufacturer; update preserva manufacturer anterior si vacío. Duplicate409; delete es desactivación. Hardware no validador semántico machining visible en handler.','Persistencia catálogo; update elimina media anterior best effort tras éxito; GET redacción de costos. No certificación CNC por tener hardware.', ['backend-go/internal/api/handlers.go:1124-1263','backend-go/internal/api/handlers.go:1701-1828'],['backend-go/internal/api/handlers_test.go'],'Catalog / engineering inputs')
extra_notes=out/'scripts/endpoint_semantic_notes.json'
if extra_notes.exists():notes.update(json.loads(extra_notes.read_text()))
notes.update(json.loads((out/'data/endpoint-fragments-identity.json').read_text()))
identity_aliases=json.loads((out/'data/endpoint-identity-aliases.json').read_text())
for key, value in identity_aliases['aliasMap'].items():alias_map[key.split(' ',1)[1]]=value['handlers'][0]
future_map={(x['method'],x['path']):x for x in identity_aliases['futureDeclarations']}
# Resolve local references without pretending the schema validates runtime requests.
def resolve(v,depth=0):
 if depth>4:return v
 if isinstance(v,dict):
  if '$ref' in v and v['$ref'].startswith('#/'):
   t=spec
   for k in v['$ref'][2:].split('/'):t=t[k]
   return {'reference':v['$ref'],'definition':resolve(t,depth+1)}
  return {k:resolve(a,depth+1) for k,a in v.items()}
 if isinstance(v,list):return [resolve(a,depth+1) for a in v]
 return v
rows=[]; files={}
for e in b['endpoints']:
 if (e['method'],e['path']) in done:continue
 op=spec.get('paths',{}).get(e['path'].removeprefix('/api'),{}).get(e['method'].lower()); hs=e.get('handlers',[]); ns=[notes[h['name']] for h in hs if h['name'] in notes]
 alias_name=alias_map.get(e['path'])
 if not hs and alias_name in notes:ns=[notes[alias_name]]
 if e['path']=='/api/health':ns=[dict(purpose='Liveness HTTP',authAndRoles='Público',validation='GET literal registrado; sin DB dependency check',sideEffects='Ninguno; devuelve200 statusok fijo',domain='Operations / liveness',evidence=['backend-go/internal/api/routes.go:104-109'],tests=[])]
 sem=bool(ns) and (len(ns)==len(hs) or bool(alias_name) or e['path']=='/api/health')
 r={'id':f'EPS-{len(rows)+1:03d}','method':e['method'],'path':e['path'],'inspectionLevel':'handler-semantic' if sem else 'schema-and-registration-only','status':'PARTIAL','purpose':ns[0]['purpose'] if sem else (op or {}).get('summary',(op or {}).get('operationId','UNKNOWN: handler semantics pending')),'schemaConfirmed':bool(op),'schema':{'operationId':op.get('operationId'),'parameters':resolve(op.get('parameters',[])),'requestBody':resolve(op.get('requestBody',{})),'responses':resolve(op.get('responses',{})),'security':op.get('security',spec.get('security',[]))} if op else {'status':'UNKNOWN','reason':'No exact method/path match; wildcard command registrations require expanded path mapping, not absence claim'},'authAndRoles':ns[0]['authAndRoles'] if sem else e.get('authorization','UNKNOWN'),'tenantScope':'Contexto auth/org según registro; scope por target explícito descrito en semántica. RLS/per-table ver database-deep-audit; no prueba live por ruta aquí.','inputValidation':ns[0]['validation'] if sem else 'UNKNOWN: contrato declarativo abajo no demuestra validación del handler','outputErrors':'Schema describe respuestas declaradas; errores de helpers/storage/middleware no son enumeración exhaustiva.','domain':ns[0]['domain'] if sem else 'UNKNOWN: no inferir ownership del nombre','sideEffects':ns[0]['sideEffects'] if sem else 'UNKNOWN: inventario de métodos storage no es prueba transaccional','handlerEvidence':hs,'registrationEvidence':e.get('evidence'),'registration':e.get('registration'),'evidence':list(dict.fromkeys([a for n in ns for a in n['evidence']])),'tests':{'status':'located-not-executed-in-this-subtask','references':list(dict.fromkeys([a for n in ns for a in n['tests'] if (src/a.split(':')[0]).exists()]))},'callers':{'status':'UNKNOWN: exact operation caller not traversed in this supplement; see web/SketchUp audit'},'unknowns':['No HTTP/DB/security execution in this supplement','Declared schema does not certify runtime validation','Full caller and per-method storage traversal remains pending']}
 r['handlerSemanticDetails']=[dict(handler=h['name'],**notes[h['name']]) for h in hs if h['name'] in notes]
 if not hs and alias_name in notes:r['handlerSemanticDetails']=[dict(handler=alias_name,**notes[alias_name])]
 r['sharedHandlerSummaryCaveat']='Top-level text summarizes first handler; handlerSemanticDetails preserves every reviewed dispatcher branch; consult method and command validation, never apply the first branch to all commands.'
 r['inventoryKind']='runtime-registration' if 'handlers' in e else 'additional-openapi-declaration'
 r['dispatchAliasOfHandler']=alias_name
 alias_details=identity_aliases['aliasMap'].get(e['method']+' '+e['path'])
 if alias_details:
  r['dispatchEvidence']=alias_details['evidence'];r['dispatchHandlers']=alias_details['handlers'];r['evidence']+=alias_details['evidence']
 future=future_map.get((e['method'],e['path']))
 if future:
  r.update(inspectionLevel='source-confirmed-future-declaration',status='FUTURE',futureDeclaration=future,authAndRoles='Not runtime: declared future contract only',tenantScope='Not implemented in registered runtime',inputValidation='Schema declaration only; no handler',sideEffects='No registered runtime implementation',domain='Future owner '+future['owner'],evidence=[future['evidence']])
 if any(h['name']=='HandleMe' for h in hs):r['relatedAudit']='data/session-scope-expiry-audit.json:AUTH-CONTRACT-01'
 r['storageDependenciesInventory']=e.get('storageMethods',[])
 r['schemaEvidence']='contracts/openapi/granete-api.v1.yaml#/paths/'+e['path'].removeprefix('/api')+'/'+e['method'].lower() if op else 'No exact OpenAPI path match after removing /api server base'
 if sem:
  direct=[]
  for h in hs:
   p=h['evidence'].split(':')[0]; text=(src/p).read_text(); match=re.search(r'func (?:\([^\n]*\) )?'+re.escape(h['name'])+r'\(',text)
   if match:
    tail=text[match.start():]; nxt=re.search(r'\nfunc ',tail[1:]); chunk=tail[:nxt.start()+1] if nxt else tail
    direct.append({'handler':h['name'],'statusSymbols':sorted(set(re.findall(r'http\.(Status\w+)',chunk))),'returnExpressions':[l.strip() for l in chunk.splitlines() if 'respondWithJSON(' in l or 'respondWithAPIError(' in l or 'respondWithError(' in l]})
  r['outputErrors']={'directSourceReturns':direct,'scope':'Shared handler may serve multiple methods; these expressions are function-wide, not all statuses for each method. Middleware/helpers/storage add errors; not exhaustive.'}
 if not r['tests']['references']:r['tests']['status']='UNKNOWN: no exact test file link retained'
 rows.append(r)
 for h in hs:
  p=h['evidence'].split(':')[0]
  if p not in files:files[p]=(src/p).read_text() if (src/p).exists() else ''
# Attach exact source-line client evidence for these reviewed families only.
client_paths=['packages/storage/src/apiWorkspaceRepository.ts','apps/web/src/services/authService.ts']
for r in rows:
 prefix=r['path'].replace('/api','').split('/{')[0]
 if len(prefix)<9:continue
 hits=[]
 for p in client_paths:
  if not (src/p).exists():continue
  for i,l in enumerate((src/p).read_text().splitlines(),1):
   if prefix in l and ('fetch' in l or 'baseUrl' in l):hits.append({'evidence':f'{p}:{i}','expression':l.strip(),'scope':'matching path prefix; verb/exact dynamic target not claimed'})
 if hits:r['callers']={'status':'source-path-prefix-matches-not-complete-callgraph','references':hits[:12]}
semantic=sum(r['inspectionLevel']=='handler-semantic' for r in rows)
result={'baseline':'316df57c7c3c9d5470b5a3f22b39fffeacfd7676','scope':'Supplement to21 critical endpoint deep rows; mixed265-row inventory consists of241 runtime registrations plus24 additional OpenAPI declarations (mostly command aliases). Row coverage is not distinct-operation count or full endpoint certification.','coverage':{'inventoryTotal':len(b['endpoints']),'runtimeRegistrations':sum('handlers' in x for x in b['endpoints']),'additionalOpenAPIDeclarations':sum('handlers' not in x for x in b['endpoints']),'countingCaveat':'Combined semantic count is inventory rows, not unique endpoint operations; wildcard aliases repeat reviewed handlers.','previousDeepRows':len(deep['endpoints']),'supplementRows':len(rows),'supplementHandlerSemanticRows':semantic,'combinedSemanticRows':semantic+len(deep['endpoints']),'remainingWithoutHandlerSemanticInspection':len(b['endpoints'])-semantic-len(deep['endpoints']),'exactSchemaMatchesInSupplement':sum(r['schemaConfirmed'] for r in rows),'runtimeInvocationsInSubtask':0},'endpoints':rows,'findings':[{'id':'EPS-F01','severity':'HIGH','classification':'CONFIRMED','status':'source-confirmed; not runtime-exploited','title':'Legacy technical workflow lacks command capability and owner checks','impact':'Authenticated non-extension member in owning tenant can reach the legacy workflow mutation without handler-level role/owner restriction; force release bypasses loading check without mandatory reason. This is legacy technical_status, not normalized DesignRelease. No cross-tenant exploit demonstrated.','evidence':['backend-go/internal/api/routes.go:526','backend-go/internal/api/internal_messages.go:101-214','backend-go/internal/storage/internal_messages.go:93-126','backend-go/internal/api/middleware.go:109-326'],'recommendation':'Add explicit command capability and project access checks; require authorized override with nonempty reason and durable atomic audit; minimize/redact response by capability.','missingProof':'Defensive routed denied-role/owner/override/audit rollback tests; runtime not executed. Full-project response has no handler redactor, but specific cost disclosure not independently proved.'}],'unknowns':['Wildcard commands require per-operation expansion; their generic registration does not prove all subcommands reviewed','Remaining schema-only rows intentionally UNKNOWN for semantic validation, callers, transaction and domain','Purchase-order retry concern inferred from no receipt+partial receive behavior; not executed, not a duplicated-stock incident claim'],'notes':[{'title':'Purchase-order receive is atomic but not necessarily idempotent','classification':'CONFIRMED source transaction / INFERRED partial retry risk','evidence':['backend-go/internal/storage/purchaseOrders.go:269-381','backend-go/internal/api/routes.go:509-515'],'description':'FOR UPDATE serializes and rejects over-receipt. A successful partial receipt still leaves remaining quantity and no request receipt shown; retry deduplication requires separate proof. This differs from the reproduced picking compensation defect.'}]}
future_count=sum(r['inspectionLevel']=='source-confirmed-future-declaration' for r in rows)
result['coverage'].update(futureDeclarationsReviewed=future_count,remainingUnreviewedRows=len(b['endpoints'])-semantic-len(deep['endpoints'])-future_count,runtimeRegistrationsReviewed=sum(('handlers' in e) and ((e['method'],e['path']) in done or any(r['method']==e['method'] and r['path']==e['path'] and r['inspectionLevel']=='handler-semantic' for r in rows)) for e in b['endpoints']),additionalCanonicalAliasesReviewed=sum(('handlers' not in e) and (e['method'],e['path']) not in future_map for e in b['endpoints']))
result['unknowns']=['Shared wildcard command and canonical alias rows repeat handlers; not independent endpoint proofs','Full caller, per-method storage, runtime negative and transaction proofs remain bounded/pending','Purchase-order partial retry concern remains inferred, not an executed incident']
(out/'data/endpoint-supplement.json').write_text(json.dumps(result,ensure_ascii=False,indent=2)+'\n')
md=['# Suplemento API: contratos declarados y semántica inspeccionada',f"\n{semantic} filas adicionales inspeccionadas semánticamente; {len(rows)} enriquecidos con contrato cuando existe coincidencia exacta. Total combinado {semantic+21}/265 con inspección semántica acotada. {future_count} son contratos futuros explícitos sin handler. {result['coverage']['remainingUnreviewedRows']} filas sin disposición estática. Ninguna llamada a producto, DB o prueba ejecutada en esta subauditoría.",'\n## Denominador\n265 filas mixtas:241 registros runtime +24 declaraciones OpenAPI adicionales. Muchas son alias del mismo wildcard; no son265 endpoints independientes.','\n## Límite de evidencia','Schema-confirmed significa declaración OpenAPI, no validación runtime. Handler-semantic significa lectura de ramas y helpers citados, no éxito HTTP ni auditoría exhaustiva de storage. Callers por prefijo y tests localizados son pistas verificables, no ejecución. Wildcards no se expandieron en operaciones sintéticas.','\n## Semántica adicional','| Registro | Propósito | Evidencia |','|---|---|---|']
for r in rows:
 if r['inspectionLevel']=='handler-semantic':md.append('| '+r['method']+' '+r['path']+' | '+r['purpose']+' | '+'; '.join(r['evidence'])+' |')
md+=['\n## EPS-F01 — HIGH, confirmado en fuente','El workflow técnico legacy carece de guard específico de capability/owner y permite override sin razón obligatoria. No es DesignRelease ni demuestra fuga cross-tenant. Ver JSON para guardas defensivas requeridas y evidencia.','\n## Contratos futuros','GET /api/organization-relationships: #453; GET /api/sales-network: #459. OpenAPI x-future-owner confirma que no son rutas runtime faltantes.','\n## Diferencia importante: atomicidad ≠ idempotencia','La recepción OC usa transacción y FOR UPDATE con saldo restante validado. Eso es más fuerte que el picking en varias solicitudes, pero no certifica deduplicación de retry parcial. Sin reproducción no se declara incidente ni se duplica BE-002.','\n## Pendientes concretos','Los alias canónicos se enlazaron a sus dispatchers reales; los dos contratos futuros están separados. Completar consumidores exactos; verificar cada error/campo contra storage y tests de ruta. Instalación tiene revisión previa en endpoint-deep/operational-flows; no se reetiquetó como cobertura nueva.','\n## Archivos','data/endpoint-supplement.json contiene input/output schemas resueltos de forma acotada, auth/roles, efectos, referencias y UNKNOWN explícitos por registro.']
(out/'evidence/endpoint-supplement.md').write_text('\n'.join(md)+'\n');print(json.dumps(result['coverage']))
