import json,hashlib
from pathlib import Path
r=Path(__file__).resolve().parents[1];s=r.parent/'source/packages/ui/src'
rows=[
('components/editor/ComponentEditorEdgesPanel.tsx',28,110,'Edge toggles patch draft flags; positive resolved preview dimensions override zero/base geometry. Same fields have checkbox fallback with labels, so the diagram is not the only input path.'),
('components/editor/ComponentEditorOptionsPanel.tsx',39,110,'Role chips normally replace the current binding, clicking selected clears it; legacy multiple roles remain visible with ambiguity alert instead of silent normalization. UI uses domain ambiguity predicate; this is not proof of resolved material parity.'),
('modules/components/ModuleEditorHardwarePanel.tsx',24,115,'Hardware rows add/remove/update by line ID. Switching fixed/role changes optionRole and clears hardwareId when returning to role mode; grid keyboard callback is forwarded, not implemented in this panel.'),
('modules/components/ModuleEditorStructurePanel.tsx',18,86,'Structure selector excludes inactive records, clears module components only when removing the structure and imports exterior dimensions when available; shows structure revision and separates body versus module component ownership.'),
('modules/components/ModuleEditorMeasuresPanel.tsx',29,95,'Commercial presets are draft-owned; import clones structure presets with fresh IDs and appends, base seeding requires three positive dimensions. canMutate disables child editor, not server authority.'),
('structures/components/StructureEditorPresetsPanel.tsx',27,101,'Dimension errors are keyed per preset; validation rejects NaN/nonpositive values, copying exterior dimensions updates three fields and clears local errors. Copy availability requires positive exterior dimensions; commercial presets remain a separate module concern.'),
('structures/components/StructureEditorComponentsPanel.tsx',31,108,'Preview delegates to resolveStructure3DPreview only when catalog input exists; empty components offer add action and populated list resolves catalog identity by componentId. This range does not prove all per-instance override mutations.'),
('projects/ExportIssueList.tsx',13,62,'Empty issue list renders nothing; nonempty alert formats module/part/field/option context and messages. It presents diagnostics, not validation itself or navigation to fixes.'),
('projects/components/ProjectItemStructureRevisionIndicator.tsx',17,29,'Closed-quote indicator forwards exact supplied pin into pinned StructureRevisionBadge; it does not select latest or resolve historical BOM.'),
('projects/components/ProjectStalenessBanner.tsx',21,84,'Domain staleness report controls alert visibility and reason labels. Change-order/release CTAs appear only when callbacks are provided; banner does not enforce production blocking.'),
('projects/components/StatusBadge.tsx',10,23,'Pure status label/class presentation delegates mapping to project helpers; decorative dot is aria-hidden. No workflow transition is implemented.'),
('projects/components/ProjectMeasureDefaults.tsx',19,67,'Editor hides for closed projects or absent update callback. Per-type depth/height changes preserve other values, remove empty/NaN fields and emit exact project ID; positive constraints/server validation remain beyond this handler.'),
('projects/components/ProjectOptionsSection.tsx',12,73,'Project default options hide when no groups, callback or editable content; select emits group code/value through detail context. Line override inheritance is promised copy but requires domain/consumer proof.'),
('modules/components/BoardFinishPickers.tsx',17,75,'Preview-only material selector explicitly says it does not save quote choices; selection defaults to first group option, shows color/grain and emits group code/material ID.'),
('modules/components/CostPreviewPanel.tsx',19,93,'Cost preview differentiates unsaved hint, blocked/missing calculation and supplied numeric breakdown. Missing group names and resolution error are visible with polite status; amounts are not recomputed by the component.'),
('modules/components/ModuleEditorCostPanel.tsx',19,54,'Existing entity passes supplied cost/error/missing-group state to preview; new entity intentionally shows save-first hint. It is a wrapper, not independent price authority.')]
rows += [
('components/editor/ComponentEditorGeneralPanel.tsx',28,100,'Identity fields patch draft; code is disabled during edit with explanatory hint. Placement-convention availability is computed only when both dimension formulas are empty; no automatic server identity enforcement is implied.'),
('components/editor/ComponentEditorGeometryPanel.tsx',100,148,'Base dimension errors reject NaN/nonpositive inputs; adding length or width formula clears the corresponding stale base-dimension error. Formula-guide focus opens assistance; final formula evaluation is outside this fragment.'),
('modules/components/ModuleEditorGeneralPanel.tsx',35,100,'General tab binds code/name/base labor to draft; existing code is locked while name remains editable. HTML required/min fields do not prove authoritative validation or cost permissions.'),
('modules/components/ModuleEditorComponentsPanel.tsx',59,115,'No structure and no own components have distinct guidance. Duplicate shallow-copies a draft component at index+1; remove filters by draft index. This is local composition, not stable physical occurrence identity.'),
('structures/components/StructureEditorGeneralPanel.tsx',25,98,'Structure code is locked on edit with identity explanation; name is required. Exterior width input clamps negative numbers to zero and remains reference geometry, with presets explicitly separate.'),
('modules/components/ModuleMeasureSection.tsx',26,100,'Commercial measure list adds fresh empty draft, updates/removes by preset ID, and disables add/import/base actions via disabled. It explains base-versus-option sales choice rather than silently adding base to the list.'),
('projects/components/StudioSelectionBar.tsx',81,158,'Empty selection hides toolbar. View fit and copy are available independently of mutation; duplicate/paste depend on canEdit and clipboard with explanatory titles, island alignment also requires at least two.'),
('projects/components/detail/ProjectDetailToolsNav.tsx',29,92,'Advanced panel buttons use aria-pressed and onToggleTools rather than ARIA tab semantics. Survey blocker count comes from domain survey helper; navigation does not itself enforce gate.')
]
rows += [
('projects/components/CostingPanel.tsx',51,115,'Time/other-cost/rate inputs require finite positive values; handlers use exact project ID but inputs clear without awaiting them. Capture availability combines permission prop with domain baseline gate. No persisted cost-success claim follows local clearing.'),
('projects/components/InstallationChecklistPanel.tsx',31,110,'Absent checklist uses generated default items; first checkbox edit seeds labels with selected label done, while later toggles use stored IDs. canEdit disables controls. This legacy checklist is separate from installation-job unit/visit closeout.'),
('projects/components/InternalCommsPanel.tsx',49,111,'Message type filter and engineer/sales owner labels use provided entities. Sending trims content, awaits callback and clears only after success, finally resets busy; selected technical transitions request comment before callback. Local error catch is absent in this range.'),
('projects/components/ProjectItemsSection.tsx',44,115,'Drag state tracks source index and above/below target midpoint with dropEffect move; end/leave clear feedback. This range does not certify final reorder command or real drag behavior; callback presence alone is not permission enforcement.'),
('projects/components/ProjectOverviewPanel.tsx',51,115,'Overview composes survey, produced-project staleness, blocking punch and field issues. It selects next open visit with legacy scheduled-date fallback, and routes to supplied engineering/production/dispatch callbacks. Derived summary is not release authority.'),
('projects/components/ProjectTotalsAside.tsx',44,114,'Totals explicitly label closed quote snapshot as frozen, expose recalculation and error states, gate missing options, and show cost lines only when showCosts. Display hiding does not redact received cost payload or prove snapshot history accuracy.'),
('projects/components/QuoteScenarioCompare.tsx',34,101,'Scenario B selection calls pure compareRoleScenario only when role and choice exist; changing role clears choice, and empty groups/items hide comparison. Actual apply/duplicate/export commands remain outside this preview fragment.'),
('projects/components/optionSelector/MaterialOptionSelectorDialog.tsx',75,110,'On opening for a group/current selection, resets search, category path, candidate and furniture scope, then locates current material category. This is picker context-reset proof, not scope application/persistence or real keyboard behavior.')
]
rows += [
('projects/components/presentation/PresentationHeader.tsx',18,63,'Header displays supplied workshop/project/customer and sale total, using dash for missing price; labelled exit delegates close. It does not calculate pricing or fetch customer data.'),
('projects/components/presentation/PresentationSummarySlide.tsx',19,96,'Summary lists quantity and preset/default/exterior dimensions with fallback on resolve failure, and offers 3D navigation when scene exists. This dimension-label helper does not consult item.customDims; custom-size presentation parity remains to be reproduced.'),
('projects/components/presentation/PresentationShortcutsModal.tsx',13,57,'Custom help overlay renders only when open, closes on backdrop-self click or labelled button and lists keyboard shortcuts. It does not itself implement focus trap, restore or Escape; parent coordination requires separate proof.'),
('projects/components/TotalsSkeleton.tsx',8,24,'Pure loading placeholder exposes role=status, labelled calculation and aria-busy with empty decorative rows; no totals are invented while waiting.')
]
rows += [
('projects/components/projectDetailContext.tsx',358,387,'Shared context transports typed project/catalog/state/callback value unchanged; useProjectDetail throws when provider is absent. It is dependency wiring, not authorization, fetching or mutation state machine.'),
('projects/components/presentation/Presentation3DViewSlide.tsx',112,165,'Client toolbar is conditional on R3F and nonempty preview, disables surface selector outside material mode and delegates capture/share. Clipboard success text comes from parent flag; no capture/export/clipboard implementation is certified here.')
]
rows += [
('components/editor/ComponentDetailView.tsx',41,98,'Detail maps four known edge sides and treats explicit zero rotation as custom rather than auto. Entity-ID change resets confirmation state. Geometry/pose are summaries, not authoritative recomputation.'),
('components/editor/ComponentFormulaGuide.tsx',13,71,'Controlled disclosure toggles help with aria-expanded and names PW/PH/PD/T/i variables; it is reference text, not evaluator or validation.'),
('components/editor/ComponentGeometry3DViewport.tsx',36,110,'Reference-container inputs clamp width/height/depth to at least one and emit updated dimensions; context and outline checkboxes affect preview only. Real renderer/performance and production geometry are not proved.'),
('components/editor/ComponentListView.tsx',39,110,'Empty filtered state derives from search/status/placement filters; reset restores active/all. Create CTA is present only for canMutate, while toolbar forwards controlled filters.'),
('components/editor/PlankEdgeDiagram.tsx',105,161,'SVG edge controls expose pressed state and descriptive labels; both click and Enter/Space call toggle only when enabled. Aspect-fit rendering is illustrative and does not mutate domain geometry.'),
('modules/components/ModuleListView.tsx',42,110,'Estimate display distinguishes absent key, null estimate and actual provided numeric value. Category selection toggles current ID while displaying derived count; this fragment does not calculate price or certify recursive hierarchy handling.'),
('structures/components/StructureListView.tsx',40,110,'Create is permission-prop gated; empty/no-results branches have different text and only true empty offers create. Cards display supplied exterior dimensions and component/preset counts, not resolved manufacturing geometry.'),
('projects/components/KitchenPlanPanel.tsx',65,94,'Legacy plan prunes layout against project items, resolves wall frames/warnings and identifies placed versus unplaced by item ID plus instance index; commits prune before callback. Full interactive commands and modern multi-space parity remain outside this range.'),
('projects/components/PresentationOptionsSlide.tsx',28,95,'Empty items/groups show no-options state. Selected options inherit item choice then project choice; dimension text resolves preset/default/exterior only, not item.customDims. Real custom-size presentation correctness remains unverified.'),
('projects/components/ProjectsListView.tsx',53,105,'List wrapper gates create and template commands by canMutate, template presence and capability callbacks. Filtered data/true-empty flags are supplied; this wrapper does not authorize or persist projects.'),
('projects/components/library/ModuleLibraryPanel.tsx',46,107,'Library navigation reads local scope/path/search with type filtering and safe fallback after malformed storage. Representative module size resolves preset, then exterior, then fixed fallback; this is placement-preview context, not actual resolved BOM.'),
('structures/components/StructureRevisionBadge.tsx',29,56,'Explicit numeric revision wins, otherwise structure revision defaults to one; pinned styling and suffix are presentational. Badge does not verify history existence or select an immutable snapshot.'),
('structures/components/agregados/StructureAgregadoFormulaLegend.tsx',8,45,'Local disclosure exposes formula-variable help with aria-expanded. W/H/D/B and fixed-thickness example text are documentation, not guaranteed formulas for every material thickness.'),
('structures/components/agregados/StructureAgregadoCard.tsx',33,95,'Card derives named template and local item key (ID else draft index), summarizes dimension/position overrides and drives controlled expand state with linked body ID. Deletion and nested override commands remain outside this fragment.')
]
out=[]
for f,a,b,claim in rows:
 p=s/f;ls=p.read_text().splitlines();assert b<=len(ls);out.append({'file':'packages/ui/src/'+f,'claim':claim,'scope':'Named component control-flow fragment only; no full acceptance or runtime proof','evidence':[{'file':'packages/ui/src/'+f,'startLine':a,'endLine':b,'excerpt':'\n'.join(ls[a-1:b]),'sha256':hashlib.sha256(p.read_bytes()).hexdigest()}],'missingProof':'Verify caller-owned update/persistence and denied/failed/dirty/keyboard paths; downstream BOM/revision/price semantics are not established by this view fragment.','runtimeStatus':'NOT_EXECUTED'})
(r/'data/web-secondary-editors.json').write_text(json.dumps({'features':out,'scope':'Read-only exact source fragments'},ensure_ascii=False,indent=2)+'\n');print(len(out))
