
!.env.example
!.vscode/extensions.json
# Build
# Dependencies
# Electron
# Env / local
# Impeccable live/critique session state (keep config tracked)
# Local agent/harness state (plans, scratch)
# Logs / caches
# OS / IDE
# Temp
# Test / coverage
*.log
*.tmp
*.tsbuildinfo
.DS_Store
.cache/
.env
.env.*
!.env.production.example
.idea/
.impeccable-live.json
.impeccable-live/
.impeccable/critique/
.impeccable/live/annotations/
.impeccable/live/server.json
.impeccable/live/sessions/
.pnpm-store/
.temp/
.turbo/
.vite/
.vscode/*
.zcode/
apps/desktop/dist-electron/
build/
# apps/desktop/build/icon.png es asset fuente de electron-builder (F075), no
# artefacto: sin esto el test de icono pasa local pero falla en checkout limpio (CI).
!apps/desktop/build/
apps/desktop/build/*
!apps/desktop/build/icon.png
coverage/
data/media/
dist/
node_modules/
npm-debug.log*
out/
pnpm-debug.log*
tmp/

# Playwright visual regression — diff output and traces (baselines ARE committed)
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
.freebuff/
# Local AI runtime state
.atl/
backend-go/server
