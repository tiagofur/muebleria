
!.env.example
!.vscode/extensions.json
# Build
# Dependencies
# Electron
# Env / local
# Impeccable live/critique session state (keep config tracked)
# Local agent/harness state (plans, scratch)
# Logs / caches
# OS / IDE
# Playwright (F052 visual baseline): keep committed baselines, ignore run artifacts
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
# Before/after evidence from manual visual audits (not test baselines)
/tests/visual/_before-after/
# Temp
# Test / coverage
*.log
*.tmp
*.tsbuildinfo
.DS_Store
.cache/
.env
.env.*
.idea/
.impeccable-live.json
.impeccable-live/
.impeccable/critique/
.impeccable/live/annotations/
.impeccable/live/server.json
.impeccable/live/sessions/
.pnpm-store/
.temp/
.turbo/
.vite/
.vscode/*
.zcode/
apps/desktop/dist-electron/
build/
coverage/
data/media/
dist/
node_modules/
npm-debug.log*
out/
pnpm-debug.log*
tmp/
